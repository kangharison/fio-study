/*
 * [한국어 설명] Windows용 sys/ioctl.h 호환 헤더 (빈 파일)
 * 컴파일 호환성을 위해 존재. Windows에서 ioctl은 DeviceIoControl로 대체됨.
 */
#ifndef IOCTL_H
#define IOCTL_H

/* This file is empty since it only needs to exist on Windows
   but isn't otherwise used */

#endif /* IOCTL_H */