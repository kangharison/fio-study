# CLAUDE.md — fio-study 프로젝트 지침

> 주석 작성 방법론(파일/함수/인라인/구조체 포맷, 핵심 원칙, 공통 작업 원칙, 커밋 규칙)은
> 상위 디렉토리의 [`../CLAUDE.md`](../CLAUDE.md)에서 공통으로 관리한다.
> 이 파일은 **fio 도메인에 특화된 지식과 작업 현황**만 정의한다.

## 프로젝트 개요

이 저장소는 [fio (Flexible I/O Tester)](https://github.com/axboe/fio) 소스 코드를 분석하고 학습하기 위한 스터디 프로젝트이다. 원본 fio 코드에 한국어 주석을 추가하여 코드 이해를 돕는 것이 목표이다.

- **원본**: fio v3.42 (Jens Axboe)
- **저장소**: https://github.com/kangharison/fio-study.git
- **브랜치**: master

## fio 핵심 I/O 흐름

```
main() [fio.c]
  → parse_options() [init.c]          # 명령줄/잡 파일 파싱
  → fio_backend() [backend.c]         # 메인 실행 루프
      → load_ioengine() [ioengines.c] # I/O 엔진 로드
      → td_io_init()                  # 엔진 초기화
      → 스레드/프로세스 생성
          → get_io_u() [io_u.c]       # I/O 유닛 할당
          → td_io_prep()              # I/O 준비
          → td_io_queue()             # I/O 제출
          → td_io_commit()            # 일괄 커밋
          → td_io_getevents()         # 완료 이벤트 수집
          → put_io_u()                # I/O 유닛 반환
      → show_run_stats() [stat.c]     # 결과 출력
```

## 핵심 자료구조 관계도

```
thread_data (잡 1개 = 스레드/프로세스 1개)
  ├── thread_options (잡 파일/CLI 옵션)
  ├── ioengine_ops (I/O 엔진 플러그인 콜백)
  │     ├── init / cleanup
  │     ├── queue / commit
  │     ├── getevents / event
  │     └── prep / open_file / close_file
  ├── io_u_queue (free / io_u_all / io_u_requeues)
  │     └── io_u (I/O 유닛 하나: offset, buflen, buf, ddir, ...)
  ├── fio_file[] (대상 파일/디바이스)
  └── thread_stat (누적 통계: lat, bw, iops)

io_log (io trace replay / write log)
verify_header (검증 데이터 메타)
```

## fio 특화 주석 요구사항

fio 코드의 특성상, 주석에 다음 사항을 반드시 포함한다:

1. **잡/스레드 모델**: 함수가 메인 프로세스, 잡 스레드, helper thread, verify 스레드 중 어디에서 실행되는지 명시
2. **I/O 엔진 플러그인 계약**: `struct ioengine_ops`의 콜백(queue/commit/getevents/event/init/cleanup)이 언제 어떤 순서로 호출되는지 설명
3. **io_u 생명주기**: free → prepped → in_flight → completed → free 상태 전이와 관련 함수(`get_io_u` / `put_io_u` / `td_io_queue`) 명시
4. **시스템 호출 맥락**: `preadv2`, `io_uring_enter`, `ioctl` 등의 호출 시 커널이 어떤 일을 수행하는지 간단히 설명
5. **OS/아키텍처 분기**: `#ifdef CONFIG_*`, `#ifdef __linux__` 분기가 있는 곳에서는 각 분기의 이유와 의미 설명
6. **verify/replay 경로**: 검증 대상 데이터(`verify_header`), io log replay 흐름이 나타나면 데이터 소스와 목적 설명
7. **통계 수집 경로**: `struct thread_stat`, `io_sample_data` 등 통계 누적 경로 설명
8. **플래그 비트 풀이**: `td->flags`, `io_u->flags`, `FIO_OPT_*` 등 비트 필드의 의미를 처음 등장 시 풀이
9. **NVMe/io_uring 스펙 참조**: 해당 코드가 NVMe 1.x 스펙 또는 io_uring UAPI의 어느 항목과 연관되는지 부연

## 디테일 수준 가이드 (fio 구체화)

| 파일 유형 | 상단 블록 | 함수 주석 | 인라인 주석 | 구조체 필드 |
|----------|----------|----------|------------|-----------|
| 핵심 코드 (fio.c, backend.c, ioengines.c, io_u.c, stat.c, init.c 등) | 매우 상세 | 모든 함수 | **모든 라인** | 모든 필드 |
| I/O 엔진 (engines/*.c) | 매우 상세 | 모든 함수 | **모든 라인** | 모든 필드 |
| 라이브러리 (lib/*.c) | 매우 상세 | 모든 함수 | **모든 라인** | 모든 필드 |
| 체크섬/해시 (crc/*.c) | 상세 | 모든 함수 | **모든 라인** | 모든 필드 |
| OS 추상화 (os/, oslib/) | 상세 | 모든 함수 | **모든 라인** | 모든 필드 |
| 아키텍처 헤더 (arch/) | 상세 | 모든 함수 | **모든 라인** | - |
| 테스트 (t/, unittests/, mock-tests/) | 상세 | 모든 함수 | **모든 라인** | 주요 구조체 |
| 프로파일 (profiles/) | 상세 | 모든 함수 | **모든 라인** | 주요 구조체 |

## 주요 용어 사전

- **td** / **thread_data**: fio의 잡 1개를 나타내는 실행 컨텍스트
- **io_u**: I/O 유닛 (하나의 read/write 요청)
- **ddir**: 데이터 방향 (`DDIR_READ`, `DDIR_WRITE`, `DDIR_TRIM`)
- **ioengine**: I/O 백엔드 플러그인 (sync, libaio, io_uring, net, rdma 등)
- **SQ/CQ**: io_uring의 Submission / Completion Queue (커널과 공유되는 링 버퍼)
- **SQE/CQE**: Submission/Completion Queue Entry
- **PRP**: Physical Region Page (NVMe의 DMA 물리 주소 리스트)
- **verify**: 쓰기 후 데이터 무결성 검증 (CRC/해시)
- **replay**: 기록된 io log를 재생하는 모드

## 주석 작업 진행 현황

> **재작업 확정 (신기준 `../CLAUDE.md` + 본 파일의 절대 규칙 적용)**
> - **기준**: "주석만으로 아키텍처 이해 가능" — 필수 4섹션 상단 블록, 모든 함수/필드/실행 라인.
> - **기존 주석 처리**: 삭제하지 않고 **그 자리에서 확장/보강**하여 신기준 충족.
> - **진행 방식**: 디렉토리 단위로 완료 후 본 섹션의 "작업 로그"와 "파일별 상태"에 즉시 기록한다.
>   새 세션이 시작되어도 **다음 착수 지점**과 **직전 파일의 품질 수준**을 이 파일만 보고 이어갈 수 있어야 한다.

### 세션 재개 프로토콜 (다음 세션에서 이 명령을 받으면 반드시 이 순서대로 수행)

1. 본 섹션 **"작업 로그"** 의 마지막 엔트리에서 **마지막으로 보강된 파일**과 **다음 착수 대상**을 확인한다.
2. 아래 **"파일별 신기준 상태"** 표에서 `◐`(부분) / `○`(미착수) 항목을 우선순위 순으로 선택한다.
3. 착수 전 해당 파일의 한국어 주석 밀도(한국어 라인 수 / 전체 라인 수)를 측정하여 현재 상태를 갱신한다.
4. 작업 완료 후 **반드시** 다음을 업데이트한다:
   - 파일별 상태 표의 해당 행 (`○`→`◐`→`●`, 한국어 라인 수 갱신)
   - "작업 로그"에 날짜/파일/요약 엔트리 추가
   - "다음 착수 대상" 포인터 갱신
5. 기존 상태 표시를 임의로 낙관적으로 올리지 않는다. 신기준 충족 판단 시 §0의 절대 규칙(상단 블록 4섹션, 모든 함수, 모든 필드 멀티라인, 모든 실행 라인 인라인)을 모두 만족해야 `●`.

### 상태 기호 정의

- `●` **신기준 완료**: 상단 블록 4섹션 + 모든 함수 주석 + 모든 필드 멀티라인 + 실행 라인 전수 인라인. 한국어/전체 비율은 파일 성격에 따라 다르나 통상 50%+.
- `◐` **부분 (구기준)**: 상단 블록·일부 함수 주석은 있으나 인라인/필드 주석이 얕음. 구기준에서 "완료"로 집계되었던 파일 대다수가 여기에 해당.
- `○` **미착수**: 한국어 주석 전무 또는 극히 일부.

### 우선순위 (신기준 재작업 순서)

1. `engines/*.c` (43) — 시작 지점. 내부 편차 큼.
2. 핵심 코어: `fio.c`, `backend.c`, `init.c`, `io_u.c`, `ioengines.c`, `stat.c`, `iolog.c`, `verify.c`, `options.c`, `server.c`
3. `lib/` (45)
4. `arch/` (18)
5. `t/` (19), `unittests/` (10)
6. `os/` (23), `crc/` (33), `oslib/` (26) — 구기준 미완료
7. 기타 최상위 헤더·유틸

### 파일별 신기준 상태 (스폿 체크 스냅샷, 2026-04-13 기준)

> 측정 방법: `rg -c '\[한국어\]' <file>` / `wc -l <file>`. 신기준 충족 여부는 비율만으로 단정할 수 없으며 실제 §0 절대 규칙 통과 여부로 판정한다. 아래 `◐`는 모두 "상단 블록은 있으나 인라인·필드 주석이 신기준 미달".

#### engines/ (43개, 모두 상단 블록 존재 — 신기준 미달)

| 파일 | 한국어/전체 | 상태 | 비고 |
|------|----------:|:---:|------|
| null.c | 신기준 | ● | **레퍼런스 템플릿** — ioengine_ops 계약 요약표 포함. 2026-04-13 보강 |
| libaio.c | 신기준 | ● | Linux 네이티브 AIO(libaio) 엔진 신기준 마감: 상단 4섹션 대폭 확장(io_setup/io_submit/io_getevents/io_cancel/io_destroy 전 5 syscall 매핑 + "prep → queue → commit → getevents → event" 5단계 엔진 내부 대응표, FIO_ASYNCIO_SYNC_TRIM·SYNC_SYNCFS·SETS_ISSUE_TIME·ATOMICWRITES 4비트 플래그 풀이, RWF_NOWAIT/ATOMIC/HIPRI/UNCACHED/APPEND UAPI 연결, userspace_reap + aio_ring mmap 직접 reap 경로 근거, DDIR_TRIM/SYNCFS 동기 폴백 이유), #include 11종 사유 전수 인라인(libaio.h의 io_context_t/iocb/io_event/IO_CMD_*/io_prep_* 매크로 카탈로그, errno의 EAGAIN/EINTR/ENOMEM/EINVAL 의미, fio.h 공급 심볼 열거, cmdprio.h의 struct cmdprio/enum mode/CMDPRIO_OPTIONS 매크로), IOCB_FLAG_IOPRIO(1<<1) ABI 폴백·aio_rw_flags→__pad2 구버전 호환 풀이, forward declaration 사유 명시, struct libaio_data 12필드 §4 멀티라인(aio_ctx/aio_events/iocbs/io_us/io_u_index/iovecs/is_pow2/entries/queued/head/tail/cmdprio 각 설정자·독자·값범위·동기화·링 동작 예시), struct libaio_options 5필드 §4(td/userspace_reap/cmdprio_options/nowait/vectored), struct aio_ring 8필드 §4(id/nr/head/tail/magic/compat_features/incompat_features/header_length/events[] 커널-유저 release/acquire 순서 보장), options[] 테이블 5엔트리(userspace_reap STR_SET + nowait BOOL + libaio_vectored BOOL + CMDPRIO_OPTIONS 일괄 전개 + NULL sentinel) 공통 규약 블록 + .name/.lname/.type/.off1/.help/.category/.group 전 필드 인라인, 전 함수 §2(ring_inc/fio_libaio_prep/fio_libaio_cmdprio_prep/fio_libaio_event/user_io_getevents/fio_libaio_getevents/fio_libaio_queue/fio_libaio_queued/fio_libaio_commit/fio_libaio_cleanup/fio_libaio_post_init/fio_libaio_init/fio_libaio_register/fio_libaio_unregister), 모든 실행 라인 인라인(ddir_sync 분기, vectored/nowait/oatomic 플래그 OR, container_of 이디엄, atomic_store_release 메모리 배리어, AIO_RING_MAGIC 검증 분기, io_submit 반환 규약(>0 부분/0/EAGAIN/ENOMEM/EINTR) 5-way 재시도 전략, 30초 stall 감지, 링 wrap 보호(entries - tail), TD_F_CHILD RCU stall 우회, io_queue_init → io_setup(2) + aio-max-nr 충돌, cmdprio mode NONE 스킵, FIO_Q_QUEUED/COMPLETED/BUSY 반환 의미), ioengine_ops 14필드 §4 멀티라인(.name/.version ABI/.flags 4비트 풀이 + 미설정 비트 의미/.init/.post_init/.prep/.queue/.commit/.getevents/.event/.cleanup/.open_file=generic/.close_file=generic/.get_file_size=generic/.options/.option_struct_size 각 설정자·호출 시점·반환 규약·본 엔진 구현 개요), constructor/destructor §2(fio_init __attribute__((constructor)) 프로세스 로더 경로·register_ioengine flist_add_tail, fio_exit __attribute__((destructor)) atexit 체인·unregister_ioengine). 2026-04-18 보강 | 227 → 252 (1222 → 1427 라인)
| sync.c | 신기준 | ● | 5개 엔진(sync/psync/vsync/pvsync/pvsync2) 단일 파일 구현 신기준 마감. 2026-04-18 보강 |
| io_uring.c | 신기준 | ● | io_uring 엔진 두 변종(ioengine_uring 블록 I/O + ioengine_uring_cmd NVMe passthru) 단일 파일 구현 신기준 마감: 상단 4섹션 기완비(SQ/CQ 링버퍼 3 mmap 영역·SETUP_SQPOLL/IOPOLL/CQSIZE 플래그 조합·REGISTER_FILES/BUFFERS/IOWQ_AFF/IOWQ_MAX_WORKERS·IORING_OP_READ/WRITE/READV/WRITEV/FSYNC/SYNC_FILE_RANGE/URING_CMD·NVME_URING_CMD_IO/IO_VEC passthru·SQE128/CQE32·smp_load_acquire/store_release 메모리 배리어·FIXEDBUFS/SQPOLL/FIXED_FILES 최적화·ZBD/FDP/cmdprio 연계), options[] 공통 규약 + 전 엔트리 인라인 기완비, struct 필드 §4 멀티라인 기완비, 본 세션 보강 — ioengine_uring 22필드·ioengine_uring_cmd 25필드 §4 멀티라인 전면 재작성(.flags 전수 풀이 — FIO_NO_OFFLOAD/FIO_ASYNCIO_SETS_ISSUE_TIME/FIO_ATOMICWRITES/FIO_MEMALIGN/FIO_RAWIO/FIO_MULTI_RANGE_TRIM/FIO_ASYNCIO_SYNC_SYNCFS + 미설정 비트 의미), constructor/destructor 본격 §2(.init_array/.fini_array ld.so·register_ioengine=flist_add_tail/unregister=flist_del_init 동적 재로딩 안전성), 10개 ZBD/FDP 위임 콜백 §2 전수 확장(get_zoned_model/report_zones/reset_wp/get_max_open_zones/finish_zone/move_zone_wp 블록+NVMe 경로 각각 + fetch_ruhs), fio_ioring_prep_md/cmd_fetch_ruhs §2 확장, ARCH_HAVE_IOURING 빌드 가드 #endif 주석 추가. 2026-04-18 보강 | 177 → 255 (3869 → 4152 라인) |
| nvme.c | 신기준 | ● | NVMe passthru 공용 헬퍼 라이브러리(io_uring_cmd/xnvme/sg 가 공유; ioengine_ops vtable 미등록): 상단 4섹션 기존 + 본 세션에 `=== 본 파일의 비-엔진 성격 ===`(공유 라이브러리·register_ioengine 미호출·정적 링크) 및 `=== ioctl(NVME_IOCTL_*) vs io_uring_cmd 경로 비교 ===`(Admin/IO 큐 동기 ioctl vs 비동기 IORING_OP_URING_CMD 매핑) 두 보강 섹션 추가. 18개 함수 §2 + 모든 실행 라인 인라인 + #include 3종 사유는 이미 신기준. ioengine_ops/생성자 부재는 비-엔진 성격이라 정상. 2026-04-18 보강 | 298 → 298 (1386 → 1406 라인) |
| sg.c | 신기준 | ● | Linux SCSI Generic(sg v3) 엔진 신기준 마감 (2026-04-18 보강): 상단 4섹션 대폭 확장(파일 역할 — SCSI CDB 전체 카탈로그 READ(10/16)/WRITE(10/16)/WRITE AND VERIFY/WRITE SAME/WRITE SAME+NDOB/WRITE STREAM/VERIFY+BYTCHK 3종/UNMAP/SYNCHRONIZE CACHE(10/16)/READ CAPACITY(10/16)/STREAM CONTROL + opcode 전수 + FUA bit3·NDOB bit0·BYTCHK bits 풀이, 전체 아키텍처 — 11콜백 호출 순서 타임라인, 동기/비동기 분기 매트릭스(/dev/sdX BLOCK→ioctl 고정 vs /dev/sgN CHAR direct=1|sync=1→write+read 동기 vs direct=0 sync=0→비동기 poll+read), FIO_SYNCIO/RAWIO/RO_NEEDS_RW_OPEN 세 비트 의미 + 미설정 비트 전수, 타 모듈과의 연결 — fio.h/optgroup.h/<scsi/sg.h>/<linux/fs.h>/poll.h 공급 심볼, 데이터 흐름 ASCII 다이어그램, 주요 함수 — 엔진 콜백 11종 + 내부 헬퍼 12종 + 구조체 4종 요약, 추가 섹션 `=== SG v3 sg_io_hdr 핵심 필드 핸드북 ===`(interface_id='S'/dxfer_direction SG_DXFER_FROM_DEV/TO_DEV/NONE/cmd_len/mx_sb_len/iovec_count/dxfer_len/dxferp/cmdp/sbp/timeout/flags SG_FLAG_DIRECT_IO 0x1+SGV4_FLAG_HIPRI 0x800/pack_id/usr_ptr 역참조 핵심/status SCSI status byte 전수/masked_status/msg_status/sb_len_wr/host_status/driver_status/resid/duration/info SG_INFO_CHECK 0x1), `=== SCSI sense data / 에러 처리 ===`(sense key 4-bit 12종 NO_SENSE/RECOVERED/NOT_READY/MEDIUM_ERROR/HARDWARE_ERROR/ILLEGAL_REQUEST/UNIT_ATTENTION/DATA_PROTECT/BLANK_CHECK/ABORTED_COMMAND/MISCOMPARE + ASC/ASCQ 예시 0x21/0x00 LBA OUT OF RANGE 등), `=== ZBD/ZAC 통합 (참고) ===`(REPORT ZONES 0x95/RESET WRITE POINTER 0x94 SA=0x04/CLOSE/FINISH/OPEN ZONE SA=0x01/02/03 libzbc 역할 명시), `=== cmdprio 통합 (참고) ===`(현재 미사용, SCSI task attribute 미노출), `=== streamid (SBC-4 streams) ===`(WRITE STREAM(16) 0x9A + STREAM CONTROL OPEN 0x34/CLOSE 0x54 + GC/WAF 효과)), #include 7종 사유 전수 인라인(stdio/stdlib/unistd/errno EAGAIN·EINTR·EIO·EINVAL·ENOMEM 각각/poll/fio.h 공급 심볼 카탈로그/optgroup.h FIO_OPT_C_ENGINE·G_SG), 매크로 6종 인라인(FIO_HAVE_SGIO·SGV4_FLAG_HIPRI 0x800·MAX_10B_LBA·SCSI_TIMEOUT_MS·MAX_SB·MAXERRDETAIL·MAXMSGCHUNK·FIO_SGIO_DEBUG), enum FIO_SG_WRITE*/VERIFY_BYTCHK* 전 8값 §4, struct sg_options 6필드 §4 멀티라인(pad/hipri/readfua/writefua/write_mode/stream_id 설정자·읽는 자·값범위·동기화), struct sgio_cmd 3필드 §4(cdb[16]/sb[MAX_SB]/nr), struct sgio_trim 3필드 §4(unmap_param/unmap_range_count/trim_io_us), struct sgio_data 9필드 §4 멀티라인(cmds/events/pfds/fd_flags/sgbuf/bs/type_checked/trim_queues/current_queue + FIO_SGIO_DEBUG trim_queue_map), options[] 테이블 공통 규약 블록 + 5엔트리(hipri/readfua/writefua/sg_write_mode 10 posval/stream_id) 전 필드 인라인(.name/.lname/.type/.off1/.help/.def/.category/.group), 전 20함수 §2(sgio_get_be16/32/64 / sgio_set_be16/32/64 / sgio_unbuffered / sgio_hdr_init / pollin_events / sg_fd_read / fio_sgio_getevents / fio_sgio_ioctl_doio / fio_sgio_rw_doio / fio_sgio_doio / fio_sgio_rw_lba / fio_sgio_prep / fio_sgio_unmap_setup / fio_sgio_queue / fio_sgio_commit / fio_sgio_event / fio_sgio_read_capacity / fio_sgio_cleanup / fio_sgio_init / fio_sgio_type_check / fio_sgio_stream_control / fio_sgio_open / fio_sgio_close / fio_sgio_errdetails / fio_sgio_get_file_size), 모든 실행 라인 인라인(CDB opcode bytes/hdr->flags|=SGV4_FLAG_HIPRI/cmdp[1]|=0x08 FUA/dxfer_direction 분기/usr_ptr container_of/SG_INFO_CHECK 분기/host_status 0x01~0x13 전수/driver_status lower nibble 0x01~0x08 + upper nibble 0x10~0x80/SCSI status 0x02~0x40 전수/MAX_10B_LBA 포화 시 RCAP(16) 폴백/service action 0x10·0x34·0x54/UNMAP parameter list 헤더 8B + 범위당 16B/BLKSSZGET ioctl/SG_GET_VERSION_NUM/generic_open_file·generic_close_file·fio_ro_check·io_u_mark_submit/complete/io_u_queued/io_u_sync_complete/fio_fill_issue_time/fio_gettime), ioengine_ops 16필드 §4 멀티라인(.name/.version ABI/.init/.prep/.queue FIO_Q_COMPLETED·QUEUED 반환 계약/.commit/.getevents/.errdetails/.event/.cleanup/.open_file/.close_file/.get_file_size/.flags FIO_SYNCIO|RAWIO|RO_NEEDS_RW_OPEN 풀이 + 미설정 비트 전수/.options/.option_struct_size), #else stub ioengine_ops 3필드 §4 + stub init §2, constructor/destructor §2 확장(fio_init .init_array ld.so 로더 경로·register_ioengine=flist_add_tail, fio_exit .fini_array atexit 체인·unregister_ioengine=flist_del_init 재적재 안전성). 543 → 568 (1842 → 2176 라인) |
| net.c | 신기준 | ● | TCP(v4/v6)/UDP(v4/v6)/Unix domain/VSOCK 네트워크 엔진 신기준 마감: 상단 4섹션 기완비, 본 세션 보강 — fio_netio_queue §2 전면 재작성(FIO_SYNCIO 계약·반환 3-way·pingpong RTT·호출 체인 ASCII), __fio_netio_queue §2(splice 선택 규칙·반환 4가지·EMSGSIZE BUSY 폴백), fio_netio_prep §2(UDP 방향 규약·TCP 면제 근거), fio_netio_open_file §2(4단계 흐름 accept/connect→UDP OPEN→close 정리), fio_netio_setup/setup_splice §2(가상 파일 등록 + netsplice의 splice+vmsplice 조합), fio_netio_terminate §2(kill(SIGTERM) self-send + EINTR 탈출 + async-signal-safety), fio_netio_connect/accept/setup_listen_inet §2 확장(proto 분기 테이블), poll_wait/set_window_size/set_mss §2(sysctl 상한·SO_RCVBUF 2배 저장·TCP_MAXSEG 스펙), fio_netio_send/recv §2(MSG_MORE/WAITALL/NOSIGNAL/DONTWAIT/OOB/PEEK/TRUNC 플래그 카탈로그), listen 백로그 인라인(SYN queue vs accept queue, net.core.somaxconn 클램프). socket/bind/listen/accept/connect/sendto/recvfrom/send/recv/setsockopt 전수, SO_REUSEADDR/REUSEPORT/RCVBUF/SNDBUF/TCP_NODELAY/TCP_MAXSEG/IP_MULTICAST_IF/TTL/IP_ADD_MEMBERSHIP, pingpong RTT, UDP LINK_OPEN/CLOSE 핸드쉐이크 + udp_seq 검증, splice/vmsplice 제로카피, FIO_SYNCIO\|DISKLESSIO\|UNIDIR\|PIPEIO\|BIT_BASED 플래그 풀이, CONFIG_LINUX_SPLICE/TCP_NODELAY/NET_WINDOWSIZE/NET_MSS/IPV6/VSOCK #ifdef 분기 전수. 2026-04-18 보강 | 542 → 567 (2493 → 2726 라인) |
| rdma.c | 신기준 | ● | RDMA(InfiniBand/RoCE/iWARP) 엔진 신기준 마감(libibverbs+librdmacm). 2026-04-18 보강 |
| ime.c | 신기준 | ● | DDN IME 엔진 세 변종(ime_psync 동기 개별/ime_psyncv 동기 vectored/ime_aio 비동기) 단일 파일 구현 신기준 마감: CONFIG_IME 빌드 게이트(configure → config-host.h → Makefile → -lim_client 링크 → options.c possible values 4단계 체인) 상단 블록 섹션, IMESDK 매크로·ime_complete_cb_t·im_client_cb_t(ime_aiocb*, int err, ssize_t bytes) 반환 규약 명시, 6개 ddir(READ/WRITE/SYNC/TRIM/DATASYNC/SYNC_FILE_RANGE) 처리 매트릭스(VERIFY→READ 재디스패치·TRIM 은 open_file 선거부·DATASYNC "wrong ddir" EINVAL), fio_ime_psync_queue/psyncv_queue/aio_queue "wrong ddir" else 분기 방어 이유, fio_ime_psyncv_end 부분 전송 분배 로직, fio_ime_aio_getevents max 초과 방지 break 이유, 세 ioengine_ops 전 필드 §4(FIO_DISKLESSIO|NODISKUTIL|RAWIO 비트 풀이), constructor/destructor §2 확장. 2026-04-18 보강 | 372 → 419 (1841 → 1913 라인) |
| xnvme.c | 신기준 | ● | xNVMe 교차 백엔드 NVMe 엔진 신기준 마감: 상단 4섹션 대폭 확장(8종 백엔드 — io_uring/io_uring_cmd/libaio/SPDK/thrpool/posix/vfio/nil 카탈로그, io_uring_cmd=NVMe passthru IORING_OP_URING_CMD, nvme.c/sg.c/io_uring.c와의 기능 중첩과 고유 가치 "하나의 코드 경로로 백엔드 스위치", ZBD/FDP 콜백이 init 이전 호출 가능해서 io_ops_data 있으면 재사용/없으면 임시 open/close 양쪽 지원, --thread=1 강제 근거 — libxnvme 백엔드 레지스트리·PCI enumeration·SPDK EAL 전역 상태 fork 불가, 시스템 콜 분기 — io_uring_cmd=io_uring_enter(2)/libaio=io_submit(2)/SPDK=유저스페이스 폴링 0콜, 데이터 흐름 offset→SLBA/NLB→cmd.nvm→PI generate(WRITE)→libxnvme→백엔드→완료→cb_pool pi_verify(READ)→iocq), #include 7종 사유 전수 인라인(libxnvme.h 불투명 포인터·CONFIG_LIBXNVME 링크, abs()로 libxnvme 음수→errno 양수 변환, verify.h VERIFY_NONE 확장LBA+PI 배타, zbd_types.h NVMe ZNS→공용 모델 매핑, dataplacement.h fio_ruhs_info, optgroup.h FIO_OPT_G_XNVME), g_serialize 전역 뮤텍스 §4, struct xnvme_fioe_fwrap 7필드 §4 멀티라인(fio_file/dev/geo/queue/ssw/lba_nbytes/md_nbytes/lba_pow2/_pad — 64B 캐시라인 STATIC_ASSERT 근거), struct xnvme_fioe_data 9필드 §4(iocq/completed/ecount/prev/cur/nopen/nallocated/iovec/md_iovec/files[] — 라운드로빈 커서·flexible array), struct xnvme_fioe_request 2필드 §4(pi_ctx/md_buf), struct xnvme_fioe_options 16필드 §4 멀티라인(hipri/sqpoll_thread/xnvme_dev_nsid/xnvme_iovec/md_per_io_size/pi_act/apptag/apptag_mask/prchk/xnvme_be/xnvme_mem/xnvme_async/xnvme_sync/xnvme_admin/xnvme_dev_subnqn — PRINFO bit3 PI Action·NQN·8종 async), options[] 15엔트리 공통 규약 블록 + 전 필드 인라인(.name/.lname/.type/.off1/.def/.help/.cb/.category/.group), 전 21함수 §2(str_pi_chk_cb/cb_pool/xnvme_opts_from_fioe/_dev_close/cleanup/_verify_options/_dev_open/init/iomem_alloc/iomem_free/io_u_init/io_u_free/event/getevents/queue/close/open/invalidate/get_max_open_zones/get_zoned_model/report_zones/reset_wp/fetch_ruhs/get_file_size/register/unregister), 모든 실행 라인 인라인(pi_flags>>3 pi_act 역추출, cpl_status→EIO, pi_verify READ+pi_act=0 조건, 라운드로빈 prev 커서 재개, -EBUSY/-EAGAIN usleep(1), offset>>ssw vs offset/lba_nbytes 분기, pi_act=1+OOB=pi_size 컨트롤러 위임 보정, PI Type1/2 16B vs 64B GUARD에서 ilbrt/cdw03 분할, Type3 REFTAG 없음, passv vs pass, FIO_Q_QUEUED/BUSY/COMPLETED 반환 규약), ioengine_ops 23필드 §4 멀티라인(.name/.version ABI/.options/.option_struct_size/.flags — FIO_DISKLESSIO|NODISKUTIL|NOEXTEND|MEMALIGN|RAWIO 5비트 풀이 + 미설정 비트/.cleanup/.init/.iomem_free/.iomem_alloc/.io_u_free/.io_u_init/.event/.getevents/.queue/.close_file/.open_file/.get_file_size/.invalidate/.get_max_open_zones/.get_zoned_model/.report_zones/.reset_wp/.fdp_fetch_ruhs), constructor/destructor §2 확장(fio_init .init_array ld.so 로더·register_ioengine=flist_add_tail, fio_exit .fini_array atexit·unregister_ioengine=flist_del_init 재적재 안전성, dlopen/dlclose 경로). 2026-04-18 보강 | 301 → 378 (2097 → 2372 라인) |
| rbd.c | 신기준 | ● | Ceph RBD(librados+librbd 2계층) 비동기 엔진 신기준 마감: 상단 4섹션 이미 완비(파일 역할·CONFIG_RBD_POLL/ENCRYPTION 분기·librbd 콜백 스레드 vs 잡 스레드 동기화 맥락·의존 모듈과 io_u 생명주기·주요 함수 요약), #include·매크로·전역 이미 인라인, fio_rbd_iou 4필드·rbd_data 7필드·rbd_options 7필드 §4 멀티라인 기완비, 18개 함수 §2(setup/init/cleanup/connect/disconnect/queue/getevents/event/iter_events/finish_aiocb/check_complete/io_u_seen/wait_complete/io_u_cmp/encryption/poll_setup/open/invalidate/io_u_init/io_u_free/_fio_setup_rbd_data/register/unregister) 기완비, 본 세션에서 options[] 테이블 공통 규약 블록 + 7엔트리의 .name/.lname/.type/.off1/.help/.def/.category/.group 전 필드 인라인 추가, ioengine_ops 14필드 §4 멀티라인 전면 재작성(.name/.version ABI/.setup/.init/.queue FIO_Q_QUEUED·COMPLETED 반환 계약/.getevents/.event/.cleanup/.open_file/.invalidate/.options/.io_u_init/.io_u_free/.option_struct_size), constructor/destructor §2 확장(__attribute__((constructor))/.init_array와 ld.so 초기화 맥락·register_ioengine=flist_add_tail·fio_exit=.fini_array atexit 체인), rados_create vs rados_create2 분기 근거(clientname의 'client.' 접두 자동 부여), rados_conf_read_file(NULL) ceph.conf 기본경로, rados_connect MON TCP+CephX 인증, rados_ioctx_create pool 바인딩, rbd_open snap=NULL head image, rbd_flush 캐시 모드(writeback/around) 확정, rbd_set_image_notification(EVENT_TYPE_EVENTFD) 완료 알림 등록, eventfd(0, EFD_SEMAPHORE) 세마포어 모드 read당 1 감소 메커니즘, rbd_poll_io_events 배치 수확, rbd_aio_{read,write,discard,flush}+rbd_aio_create_completion+rbd_aio_get_return_value+rbd_aio_release 전 API 인라인, busy_poll의 wait 전환 로직, sort_events qsort+rbd_aio_wait_for_complete age-based fairness, LUKS1/LUKS2 rbd_encryption_load, CONFIG_RBD_INVAL/CONFIG_RBD_POLL/CONFIG_RBD_ENCRYPTION 3종 #ifdef 분기 근거. | 156 → 193 (1260 → 1375 라인)
| splice.c | 신기준 | ● | Linux splice(2)/vmsplice(2) 제로카피 엔진: 파일→파이프→사용자 iovec 페이지 참조 이동(실제 복사 없음), SPLICE_F_MOVE/MORE/NONBLOCK/GIFT 4비트 풀이, 파이프 버퍼 64KiB(F_SETPIPE_SZ), READ=splice(file→pipe)+vmsplice(pipe→user), WRITE=vmsplice(user→pipe GIFT)+splice(pipe→file), vmsplice_to_user+mmap 덮어쓰기 트릭(페이지 정렬 요구), -EBADF→엔진 폴백 신호, O_DIRECT 비호환, spliceio_data 3필드 §4, 7함수 §2, ioengine_ops 9필드 §4(FIO_SYNCIO|FIO_PIPEIO 비트), constructor/destructor §2. 2026-04-18 보강 | 153 → 596 (449→1008라인) |
| solarisaio.c | 신기준 | ● | Solaris 네이티브 AIO(aioread/aiowrite/aiowait/aio_result_t) 엔진 신기준 마감: 상단 4섹션 확장(POSIX AIO 와의 API 차이 — aio_result_t 는 사용자 구조체에 커널이 직접 기록, aiowait 가 "아무거나 완료" 반환해 aio_suspend 식 리스트 관리 불필요, SIGIO vs aiowait 두 완료 모드, DDIR_SYNC/DATASYNC 는 aio_fsync 미지원이라 드레인+fsync/fdatasync 동기 폴백), #include 7종 사유 전수 인라인(sys/asynch.h 에서 공급되는 aio_result_t/aioread/aiowrite/aiowait/AIO_INPROGRESS/MAXASYNCHIO 전수 카탈로그), struct solarisaio_data 4필드(aio_events/aio_pending/nr/max_depth) §4 멀티라인(설정자/독자/값범위/동기화·SIGIO 경로의 write_barrier 근거), 전 11함수 §2(prep/wait_for_event/getevents/event/queue/cleanup/sigio/init_sigio/init/register/unregister), 모든 실행 라인 인라인(container_of 이디엄, aiowait 반환 규약 -1/NULL/valid 3분기, EINVAL 정상 조기반환 근거, timespec→timeval ns/us 다운컨버전, MAXASYNCHIO 클램프 이유, DDIR_SYNC 드레인+fsync 순서, FIO_Q_BUSY/COMPLETED/QUEUED 반환 의미, 비동기 시그널 컨텍스트 재진입 안전성, SA_RESTART 효과), USE_SIGNAL_COMPLETIONS 빌드 분기 조건·장단점·async-signal-safety 주의, ioengine_ops 11필드 §4(.flags=0 의미 — FIO_SYNCIO/DISKLESSIO/RAWIO/NOEXTEND/MEMALIGN/BARRIER/UNIDIR/NODISKUTIL/ASYNCIO_SYNC_TRIM/SYNCFS 모두 미설정 이유 전수), constructor/destructor §2(fio_init/fio_exit GCC constructor/destructor 속성, engine_list 등록 메커니즘). POSIX AIO(posixaio.c) 와의 차이를 상단 4섹션·#include <sys/asynch.h>·wait_for_event·queue(DDIR_SYNC) 네 지점에서 교차 설명. 2026-04-18 보강 |
| mtd.c | 신기준 | ● | MTD(Memory Technology Device) /dev/mtdN NOR/NAND 캐릭터 디바이스 엔진 신기준 마감: 상단 4섹션 기완비(erase block 경계 분할·skip_bad·libmtd 래퍼), #include 8종 사유·전역 desc·구조체 2종(fio_mtd_data/fio_mtd_options) §4 멀티라인 기완비, 8함수 §2(maybe_mark_bad/is_bad/queue/open_file/close_file/get_file_size/register/unregister) 기완비, 본 세션 보강 — options[] 테이블 공통 규약 블록(`.name/.lname/.type/.off1/.help/.hide/.def/.category/.group/.interval` 의미) + skip_bad 엔트리 전 필드 인라인, NULL sentinel 역할 명시, ioengine_ops 9필드 §4 멀티라인(.name/.version ABI/.queue FIO_Q_COMPLETED 반환 규약/.open_file/.close_file/.get_file_size/.flags FIO_SYNCIO\|FIO_NOEXTEND 풀이 + 미설정 13비트 전수 의미 — DISKLESSIO/RAWIO/NOIO/PIPEIO/BARRIER/UNIDIR/NODISKUTIL/MEMALIGN/ASYNCIO_SYNC_TRIM/SETS_ISSUE_TIME/RO/MULTI_RANGE_TRIM/NO_OFFLOAD/.options/.option_struct_size), constructor/destructor 본격 §2(fio_init .init_array ld.so·fio_exit .fini_array atexit·libmtd_open/close 페어링 + desc=NULL 댕글링 방지 근거·register_ioengine=flist_add_tail/unregister=flist_del_init 동적 재로딩 안전성). 2026-04-18 보강 | 61 → 84 (337 → 448 라인) |
| mmap.c | 신기준 | ● | mmap(2)+memcpy 동기 엔진, 32비트 MMAP_TOTAL_SZ=1GiB per-job 상한 + partial_mmap 폴백, THP 분기(MADV_HUGEPAGE+MAP_PRIVATE), fadvise_hint(RANDOM/SEQUENTIAL) posix_madvise 매핑, odirect 에뮬레이션(msync+POSIX_MADV_DONTNEED), fio_mmap_data 3필드·mmap_options 2필드 §4, ioengine_ops 전 필드 §4(FIO_SYNCIO|FIO_NOEXTEND 풀이). 2026-04-17 보강 |
| libpmem.c | 신기준 | ● | PMDK libpmem 기반 DAX PMEM mmap+memcpy 동기 엔진, pmem_map_file로 파일→가상주소 매핑 후 memcpy/pmem_memcpy(NT|T, DRAIN|NODRAIN)/pmem_drain 3분기, sync=1→F_MEM_NODRAIN 해제, direct=1→F_MEM_NONTEMPORAL 캐시 우회, rw_min_bs & page_mask + fsync/fdatasync 조합 거절, fio_libpmem_data 3필드 §4(libpmem_ptr/libpmem_sz/libpmem_off), ioengine_ops 전 필드 §4(FIO_SYNCIO|RAWIO|DISKLESSIO|NOEXTEND|NODISKUTIL|BARRIER|MEMALIGN 비트 풀이). 2026-04-18 보강 |
| e4defrag.c | 신기준 | ● | ext4 EXT4_IOC_MOVE_EXT ioctl로 extent 교체 벤치마크, _IOWR('f',15,struct move_extent) UAPI 폴백, 도너 fd+fallocate(inplace=0 사전/inplace=1 매 I/O)+fstat st_blksize 취득, orig_start=offset/bsz·len=올림(end/bsz)-start 블록 환산, moved_len*bsz→io_u->resid 부분이동 보고, DDIR_WRITE만 허용(READ/TRIM 거부), e4defrag_data 2필드·e4defrag_options 3필드·move_extent 6필드 §4, options[] 테이블 .name/.lname/.type/.off1/.help/.category/.group/.minval/.maxval 전 필드 인라인, ioengine_ops 전 필드 §4, constructor/destructor §2. 2026-04-18 보강 |
| rados.c | 신기준 | ● | Ceph RADOS 오브젝트 엔진(librados 단독 — RBD 2계층 아님): fio_file↔오브젝트 1:1 매핑, rados_create/create2(clustername 분기)/conf_read_file/connect(MON TCP+CephX+OSDMap)/ioctx_create(pool 바인딩) 5단계, rados_aio_create_completion+rados_aio_read/write/write_op_zero(TRIM 에뮬)/flush_async, complete_callback→ops_completed++ 카운팅, rados_data 7필드·fio_rados_iou 4필드·rados_options 7필드 §4, 14함수 §2, ioengine_ops 14필드 §4(.flags=FIO_DISKLESSIO 비트 풀이), use_thread=1 강제(librados fork 비안전). 2026-04-18 보강 |
| posixaio.c | 신기준 | ● | POSIX AIO(aio_read/write/fsync)+aio_error 폴링+aio_suspend 대기 루프, FIO_ASYNCIO_SYNC_TRIM|SYNCFS 동기 폴백, EAGAIN→FIO_Q_BUSY 백프레셔(OSX 큐 포화), CONFIG_POSIXAIO_FSYNC #ifdef 분기, SUSPEND_ENTRIES=8 스택 버퍼 배치 수확, posixaio_data 2필드 §4, ioengine_ops 전 필드 §4. 2026-04-17 보강 |
| nfs.c | 신기준 | ● | libnfs 유저스페이스 NFS 클라이언트 엔진, nfs://server/export/path URL→nfs_parse_url_dir→nfs_mount RPC(MOUNT+NFSv3/v4), poll(2)+nfs_which_events→nfs_service 이벤트 루프, nfs_callback→io_u 완료 연결, PROCNFS3_READ/WRITE/CREATE/NULL RPC, LIBNFS_API_V2 #ifdef 분기(v1 READ memcpy 필요/v2 direct-io), nfs_data 2필드·fio_libnfs_options 8필드·enum nfs_op_type 3값 §4, ioengine_ops 전 12필드 §4(FIO_DISKLESSIO|NOEXTEND|NODISKUTIL 풀이), use_thread=0 강제 이유(libnfs 스레드 정리 hang). 2026-04-18 보강 |
| windowsaio.c | 신기준 | ● | Windows IOCP 비동기 엔진: CreateIoCompletionPort/GetQueuedCompletionStatus[Ex] 기반, 두 완료 수집 모드(별도 IoCompletionRoutine 스레드 + auto-reset Event 기반 wake vs no_completion_thread=1 나른 스레드 GQCS-Ex 16개 배치 수거), FILE_FLAG_OVERLAPPED 필수 + POSIX_SEMANTICS/NO_BUFFERING(odirect)/WRITE_THROUGH(sync_io)/RANDOM_ACCESS|SEQUENTIAL_SCAN(fadvise_hint) 플래그 조합, DDIR_SYNC/DATASYNC/SYNC_FILE_RANGE → FlushFileBuffers 동기 처리, DDIR_TRIM 미지원, 캐시 무효화는 FILE_FLAG_NO_BUFFERING로 잠시 열었다 닫는 트릭, CONTAINING_RECORD 이디엄으로 OVERLAPPED*→fio_overlapped 복원, win_to_posix_error로 errno 변환, timeout_expired의 GetTickCount wrap-safe, fio_overlapped 3필드(OVERLAPPED 세부 필드 포함)·windowsaio_data 5필드·thread_ctx 2필드·windowsaio_options 2필드 §4, options[] 테이블 .name/.lname/.type/.off1/.help/.category/.group 전 필드 인라인, ioengine_ops 13필드 §4(.flags=0 의미 전수 풀이 — FIO_SYNCIO/DISKLESSIO/NOEXTEND/RAWIO/MEMALIGN/BARRIER/UNIDIR/NODISKUTIL/FAKEIO 모두 미세트 이유). 2026-04-18 보강 |
| libzbc.c | 신기준 | ● | libzbc ZBC/ZAC 엔진 (SMR/ZNS 호스트 관리형 존 디스크): zbc_open/close_dev·report_zones·reset_zone·finish_zone·pread·pwrite·flush·errno/sk_str/asc_ascq_str API, 존 타입 CNV/SWR/SWP + 존 상태 머신(EMPTY/IMP_OPEN/EXP_OPEN/CLOSED/FULL/RDONLY/OFFLINE/NOT_WP), Host-Managed(HM) vs Host-Aware(HA) 분기, zbd_types.h/zbd.h 통해 fio zbd 코어에 get_zoned_model/report_zones/reset_wp/move_zone_wp/finish_zone/get_max_open_zones 콜백 제공, <<9/>>9 512B 섹터 변환, SCSI READ(16)/WRITE(16)/SYNCHRONIZE CACHE(16) CDB 매핑, 지연 오픈 패턴(report_zones·get_zoned_model이 open_file보다 먼저), ALL_ZONES RESET 최적화, libzbc_data 4필드 §4(zdev/model/nr_sectors/max_open_seq_req), ioengine_ops 14필드 §4(FIO_SYNCIO|FIO_NOEXTEND|FIO_RAWIO 비트 풀이 + 미설정 비트 의미). 2026-04-18 보강 | 78 → 172
| nbd.c | 신기준 | ● | libnbd 기반 NBD 클라이언트 엔진, nbd://·nbd+unix:// URI, FIO_DISKLESSIO 가상 파일(add_file "nbd")+setup 동기 프로브로 export size 취득 후 close, init 잡별 실제 AIO 커넥션, aio_pread/pwrite/trim/flush 4종 ddir 매핑, nbd_completion_callback+cmd_completed→completed 배열 append, nbd_poll+retire_commands 2단 수확, NBD_MAX_REQUEST_SIZE=64MiB assert, nbd_data 4필드·nbd_options 2필드 §4, ioengine_ops 전 필드 §4. 2026-04-17 보강 |
| libblkio.c | 신기준 | ● | libblkio 통합 블록 엔진(io_uring/nvme-io_uring/virtio-blk-vhost-user·vdpa/vfio-user), proc_state 공유 blkio + per-thread blkioq 구조, 3종 wait_mode(block blkioq_do_io / eventfd blocking read / loop busy-poll), hipri poll queue(blkio_get_poll_queue 인덱싱), mem-region-alignment 정렬 + iomem_alloc/post_init 두 경로, ddir→blkioq_{read,write,readv,writev,discard,write_zeroes,flush} 매핑, FIO_Q_QUEUED 비동기 계약, threaded subjob 옵션 호환성 검증(first_threaded_subjob 스냅샷), 전 함수 §2, 전 필드 §4. 2026-04-15 보강 |
| libiscsi.c | 신기준 | ● | 유저스페이스 iSCSI initiator(libiscsi) 엔진: iscsi_context/iscsi_url_parse/full_connect_sync/READ CAPACITY(16) 초기화, READ16/WRITE16/SYNCHRONIZECACHE16 CDB 매핑, poll(2)+iscsi_which_events+iscsi_service 이벤트 루프, iscsi_cb→complete_events 축적, 3구조체(iscsi_task/iscsi_lun/iscsi_info) 전 필드 §4. 2026-04-15 보강 |
| libhdfs.c | 신기준 | ● | Hadoop HDFS libhdfs JNI 엔진: append-only 우회 위해 chunk_size 단위 다중 파일 프리할당, offset→(f_id, intra-offset) 매핑, hdfsBuilder→NameNode 접속, single_instance FileSystem 캐시 공유 vs ForceNewInstance 격리, readDirect vs hdfsRead, hdfsFlush 내구성 제한(hflush/hsync 아님), FIO_SYNCIO+FIO_DISKLESSIO+FIO_NODISKUTIL 플래그, hdfsio_data 3필드·hdfsio_options 7필드 §4, 전 함수 §2, 옵션 테이블/ioengine_ops 전 필드 인라인. 2026-04-15 보강 |
| libcufile.c | 신기준 | ● | NVIDIA GPUDirect Storage(GDS) 엔진: 전역 refcount+mutex로 첫 잡 cuFileDriverOpen/마지막 잡 cuFileDriverClose, cuFileHandleRegister(fd→CUfileHandle, OPAQUE_FD 타입)로 nvidia-fs가 FS 블록맵 캐시, cuFileBufRegister로 GPU 버퍼 DMA pin(없으면 bounce 버퍼), cuFileRead/Write가 NVMe PRP를 GPU BAR1 물리주소로 채워 P2P DMA(호스트 페이지캐시 우회), cuda_io=cufile vs posix(pread/pwrite+cudaMemcpy baseline), 4KB 정렬 필수(NVMe LBA+nvidia-fs DMA 제약)·LOGGED 비트마스크로 경고 1회, pre_write/post_read의 verify(H2D)/POSIX(D2H·H2D) cudaMemcpy 오버헤드 재현, gpu_dev_ids 콜론 구분+subjob_number%id_count 라운드로빈+cudaSetDevice, libcufile_options 8필드·fio_libcufile_data 2필드 §4, 모든 함수 §2, ioengine_ops 전 필드 §4(FIO_SYNCIO 동기 계약). 2026-04-15 보강 |
| http.c | 신기준 | ● | libcurl easy 기반 S3/Swift/WebDAV 오브젝트 엔진, AWS SigV4 HMAC-SHA256 파생 체인(kSecret→kDate→kRegion→kService→kSigning→sig), SSE-C/STS/Storage-Class 헤더, DDIR→PUT/GET/DELETE 매핑, object_mode(block/range)+Range 헤더, _http_read/_http_write/_http_seek 콜백, CURLAUTH_ANY, CONFIG_HAVE_OPAQUE_HMAC_CTX 분기, FIO_SYNCIO+FIO_DISKLESSIO 플래그 풀이. 2026-04-15 보강 |
| dfs.c | 신기준 | ● | DAOS pool/container/DFS 마운트 체인, daos_event_t+EQ 비동기, container_of로 io_u 복원, FIO_Q_QUEUED 계약. 2026-04-15 보강 |
| cpu.c | 신기준 | ● | CPU-burner 엔진(FIO_SYNCIO+DISKLESSIO+NOIO), noisy-neighbor 시뮬레이션, noop/qsort 모드, cpuload·thinktime 재교정, MWC RNG. 2026-04-15 보강 |
| cmdprio.c | 신기준 | ● | libaio/io_uring/sg 공유 우선순위 유틸: ioprio_set(2)/IOPRIO_CLASS_RT·BE·IDLE/HINT_DEV_DURATION_LIMIT, PERC vs BSSPLIT 모드, bssplit CDF 추첨, clat_prio_index 사전 할당, cmdprio_ops 연계. 2026-04-15 보강 |
| exec.c | 443→690여 라인 | ● | fork/execvp/waitpid 커널 의미 명시. 2026-04-13 보강 |
| falloc.c | 신기준 | ● | fallocate(2) 모드 플래그(KEEP_SIZE/PUNCH_HOLE) 풀이, O_RDWR 필수 근거, filehash 재시도 루프, ioengine_ops 전 필드 §4. 2026-04-15 보강 |
| ftruncate.c | 신기준 | ● | ftruncate(2)로 파일 크기 변경 메타연산 측정. FIO_SYNCIO/FAKEIO/SYNCFS 플래그, ddir 분기, FIO_Q_COMPLETED 동기 계약. 2026-04-15 보강 |
| skeleton_external.c | 신기준 | ● | 외부 엔진 .so 템플릿(dlopen/dlsym "ioengine" 심볼), fio_skeleton_options 2필드·ioengine_ops 전 필드 §4, ZBD 콜백 4종 §2, FIO_Q_COMPLETED no-op 계약 설명. 2026-04-17 보강 |
| fileoperations.c | 신기준 | ● | 파일시스템 메타연산 측정 엔진. 2026-04-13 보강 |
| glusterfs.c | 신기준 | ● | sync/async 공유 공통 유틸. 2026-04-13 보강 |
| glusterfs_sync.c | 신기준 | ● | FIO_SYNCIO+FIO_DISKLESSIO, CONFIG_GF_NEW_API 분기 명시. 2026-04-13 보강 |
| glusterfs_async.c | 239→500여 라인 | ● | ioengine 비동기 계약(queue→FIO_Q_QUEUED/event/getevents). 2026-04-13 보강 |
| dev-dax.c | 신기준 | ● | DAX 페이지캐시 우회·CLWB/SFENCE 영속성 명시. 2026-04-13 보강 |
| nvme.h | 신기준 | ● | NVMe passthru UAPI/구조체 헤더 신기준 마감: 기존 ◐(축약형 `=== 개요/주요 기능 영역/주요 구조체 ===` + 모든 필드 단일 라인 BAD 주석)을 본 세션에 (a) 정확한 4섹션 + 추가 3섹션(`=== NVMe 명령 CDW 레이아웃 핸드북 ===` Read/Write/Flush/DSM/Zone Mgmt/Identify CDW0~15 비트 슬롯 전수 + `=== Protection Information(PI) 동작 원리 ===` 16B T10 DIF/64B CRC64/PRACT/PRCHK_*/REFTAG/APPTAG/Escape 매직 + `=== ZNS / FDP / Streams 요약 ===`)으로 재작성, (b) #include 2종 사유 인라인 확장(linux/nvme_ioctl.h UAPI/../fio.h 카탈로그), (c) struct nvme_uring_cmd 18필드 §4 멀티라인(opcode/flags/rsvd1=CID 자리/nsid/cdw2/cdw3=64b 가드 reftag 상위 16b/metadata MPTR/addr=PRP or iovec/metadata_len/data_len/cdw10~15 명령별 의미/timeout_ms/rsvd2 — 각 NVMe 스펙 비트 슬롯·설정자·읽는 자·동기화), (d) 매크로 11종 인라인 풀이(NVME_URING_CMD_IO[_VEC] _IOWR magic, NVME_DEFAULT_IOCTL_TIMEOUT 의 io_timeout 위임, NVME_IDENTIFY_DATA_SIZE/CSI_SHIFT, NVME_PI_APP/REF_DISABLE escape, NVME_ZNS_ZRA/ZRAS/ZSA, NVME_ZONE_TYPE_SEQWRITE_REQ, NVME_ATTRIBUTE_DEALLOCATE), (e) enum 7종 §4(nvme_identify_cns 4값/nvme_csi 3값/nvme_admin_opcode 1값/nvme_io_opcode 11값/nvme_zns_zs 7값/nvme_id_ctrl_ctratt ELBAS bit15/elbaf 비트 풀이/가드 타입 16B/32B/64B), (f) struct §4 멀티라인 16종(nvme_data 9필드/nvme_pi_data 4필드/nvme_lbaf 3필드/nvme_16b_guard_pif 3필드/nvme_64b_guard_pif 3필드/nvme_id_ns 30+필드 fio 사용 강조 + 미사용 그룹 코멘트/nvme_id_psd 14필드/nvme_id_ctrl 50+필드 + Fabrics Only 그룹/nvme_nvm_id_ns 4필드 elbaf 활용/nvme_zns_lbafe 3필드/nvme_zns_id_ns 14필드 mor 강조/nvme_zns_desc 9필드 zt/zs/zcap/zslba/wp/nvme_zone_report 3필드/nvme_fdp_ruh_status_desc 5필드/nvme_fdp_ruh_status 3필드/nvme_dsm_range 3필드 nlb 1-based 주의/nvme_dsm 2필드/nvme_cmd_ext_io_opts 3필드), (g) inline 함수 6종 §2 + 모든 실행 라인 인라인(ilog2 floor(log2)+0→-1, put_unaligned_be48/get_unaligned_be48 6 bytes BE 풀이, fio_nvme_pi_ref_escape 0xFF×6 매직, get_slba lba_ext 분기 시프트 가속, get_nlb 0-based -1 보정 + DSM 1-based 호출자 +1 보정 주의), (h) 외부 링크 함수 선언 10종(fio_nvme_iomgmt_ruhs/get_info/uring_cmd_prep/pi_fill/generate_guard/pi_verify/get_zoned_model/report_zones/reset_wp/get_max_open_zones)에 caller/return 한 줄 §2. 2026-04-18 보강 | 39 → 275 (621 → 1425 라인) |
| cmdprio.h | 신기준 | ● | libaio/io_uring/sg 공유 ioprio 우선순위 믹스 헤더 신기준 마감: 상단 4섹션 재작성(파일 역할 — 세 엔진 공유 계약, 전체 아키텍처 — init→queue 매 io_u→cleanup 수명 + libaio `iocb->aio_reqprio`/io_uring `sqe->ioprio`/sg `ioprio_set(2)` 세 경로 분기, 타 모듈 — ../fio.h/../optgroup.h/../lib/rand.h/FIO_HAVE_IOPRIO_CLASS 분기 명시, 주요 함수·구조체 요약, Linux ioprio (class<<13)\|level+hint 비트 레이아웃 섹션), #include 2종 사유 인라인, CMDPRIO_RWDIR_CNT 매크로 인라인, enum 3값 §4(NONE/PERC/BSSPLIT — 배타 검증 이유), struct cmdprio_prio 3필드 §4(prio/perc/clat_prio_index — 설정자·독자·값범위), struct cmdprio_bsprio 4필드 §4(bs/tot_perc/nr_prios/prios), struct cmdprio_bsprio_desc 2필드 §4, struct cmdprio_options 5필드 §4(percentage/class/level/hint/bssplit_str — ddir별 배열 off1/off2 구조·FIO_OPT_STR_STORE strdup 소유), struct cmdprio 4필드 §4(options/perc_entry/bsprio_desc/mode), CMDPRIO_OPTIONS 매크로 5엔트리 공통 규약(.name/.lname/.type/.off1/.off2/.minval/.maxval/.help/.interval/.category/.group) 전 필드 인라인 + UNSUPPORTED 폴백 경로, fio_cmdprio_set_ioprio/init/cleanup 각각 §2(@param/@return/호출 체인/동작 단계). 2026-04-18 보강 | 11 → 54 (250 → 373 라인) |
| gfapi.h | 신기준 | ● | GlusterFS gfapi 세 엔진(glusterfs/glusterfs_sync/glusterfs_async) 공유 헤더 신기준 마감: 상단 4섹션(파일 역할 — 볼륨 마운트/파일 오픈/통계 수집 공통 계약, 전체 아키텍처 — setup→init→get_file_size→open_file→queue/event/getevents→close_file→cleanup 타임라인 + libgfapi TCP 24007/24008 접속, 타 모듈 — `<glusterfs/api/glfs.h>`/`../fio.h`/간접 FIO_OPT_G_GFAPI + single_instance 시 hash table 참조 카운트, 주요 함수/구조체 요약), #include 2종 사유 인라인, struct gf_options 4필드 §4 멀티라인(pad off1==0 예약 회피/gf_vol/gf_brick glfs_set_volfile_server(fs,"tcp",brick,24007)/gf_single_instance 해시/pthread_mutex), struct gf_data 3필드 §4(fs glfs_t 마운트 핸들/fd glfs_fd_t 파일 핸들/aio_events 비동기 완료 링 버퍼 libgfapi 스레드풀 동기화), 6 extern 선언(gfapi_options/fio_gf_setup/cleanup/open_file/close_file/get_file_size/unlink_file) 각각 §2 한 줄 요약(호출 시점·반환 규약·피호출 libgfapi API). 2026-04-18 보강 | 9 → 19 (37 → 138 라인) |

#### 핵심 코어 (최상위)

| 파일 | 한국어/전체 | 상태 | 비고 |
|------|----------:|:---:|------|
| backend.c | 1743/5052 (34%) | ◐ | 2026-04-18 보강 — 상단 4섹션 재작성 + td->runstate 전이 다이어그램(TD_NOT_CREATED→CREATED→INITIALIZED→RAMP→RUNNING/VERIFYING/FSYNCING→FINISHING→EXITED→REAPED) 추가, #include 11 시스템 + 19 내부 헤더 사유 전수, 전역 18개(startup_sem/cgroup_list/cgroup_mnt/exit_value/fio_abort/nr_process/nr_thread/agg_io_log/groupid/thread_number/nr_segments/cur_segment/stat_number/temp_stall_ts/done_secs/overlap_check/write_bw_log_name/JOB_START_TIMEOUT) + struct fork_data §4 멀티라인, 함수 §2 헤더 41/60+ 업그레이드(sig_int/sig_break/sig_show_status/set_sig_handlers/__check_min_rate/check_min_rate/fio_io_sync/fio_file_fsync/fio_syncfs/update_ts_cache/runtime_exceeded/update_runtime/break_on_this_error/check_update_rusage/wait_for_completions/io_queue_event/io_in_polling/unlink_all_files/in_flight_overlap/io_u_submit/do_verify/exceeds_number_ios/io_bytes/io_issue_bytes/io_complete_bytes_exceeded/usec_for_io/init_thinktime/handle_thinktime/log_inflight/invalidate_inflight/clear_inflight/do_io/init_inflight_logging/free_inflight_logging/cleanup_io_u/init_io_u/init_io_u_buffers/set_ioscheduler/switch_ioscheduler/keep_running/exec_string/do_dry_run/thread_main/reap_threads/trigger_*/fio_verify_load_state/do_usleep/check_mount_writes/waitee_running/run_threads/free_disk_util/fio_backend). **미완료**: do_io/thread_main/run_threads 본문 수백 라인의 §3 전수 인라인(기존 한 줄 요약 → "왜 이 조건인가" 확장) + __check_min_rate/handle_thinktime 본문. 에이전트 자체 평가 70-80% 신기준 — 본문 인라인 보강 1회 더 패스 시 ● 승격 예정 |
| io_u.c | 1596/4577 (35%) | ◐ | 2026-04-18 재보강: 82개 함수 전수 §2 재작성(random_map_free/mark_random_map/last_block/__get_next_rand_offset_sprandom/__get_next_rand_offset/__get_next_rand_offset_zipf/pareto/gauss/zoned_abs/zoned/get_next_rand_offset/should_do_random/loop_cache_invalidate/get_next_rand_block/get_next_seq_offset/get_next_block/get_next_offset/io_u_fits/get_next_buflen/set_rwmix_bytes/get_rand_ddir/io_u_quiesce/rate_ddir/get_rw_ddir/set_rw_ddir/put_file_log/put_io_u/io_u_clear_inflight_flags/clear_io_u/requeue_io_u/setup_strided_zone_mode/fill_multi_range_io_u/fill_io_u/__io_u_mark_map/io_u_mark_submit/complete/depth/io_u_mark_lat_{n,u,m}sec/io_u_mark_latency/__get_next_fileno_rand/get_next_file_rand/rr/__get_next_file/get_next_file/set_io_u_file/lat_fatal/lat_new_cycle/__lat_target_failed/lat_target_failed/lat_target_init/reset/success/check/queue_full/__get_io_u/check_get_trim/check_get_verify/small_content_scramble/get_io_u/__io_u_log_error/io_u_log_error/gtod_reduce/trim_block_info/account_io_completion/file_log_write_comp/should_account/io_completed/init_icd/ios_completed/io_u_update_bytes_done/io_u_sync_complete/io_u_queued_complete/io_u_queued/get_buf_state/save_buf_state/fill_io_buffer/io_u_fill_buffer/do_sync_file_range/do_io_u_sync/do_io_u_trim). 핵심 생명주기 함수 6종(get_io_u/__get_io_u/put_io_u/io_completed/fill_io_u/set_io_u_file)에 ASCII 상태 전이 다이어그램 + 호출 체인 + 에러 경로 삽입. 랜덤 분포 6종(uniform/sprandom/zipf/pareto/gauss/zoned/zoned_abs)에 수학적 배경 + 파라미터 의미. 미완료: 함수 본문 라인별 §3 인라인 보강(일부 핵심 함수 mark_random_map/__get_next_rand_offset 만 상세화, 나머지는 기존 shallow 유지). 1060 → 1596 라인, 3874 → 4577 라인. |
| fio.c | 신기준 | ● | main() 기동 뼈대: initialize_fio→sk_key→parse_options→setvbuf(_IOLBF)→fio_time_init→(fio_start_all_clients+fio_handle_clients | fio_backend)→sk_key_destroy→deinitialize_fio. goto done_key/done 부분정리, nr_clients 분기, 시그널/terminate_threads는 backend.c 위임 명시. 2026-04-15 보강 |
| init.c | 1652/5100 (32%) | ● | 2026-04-18 보강. 상단 블록 4섹션을 (1)CLI/INI/공유 SHM/fixup 네 대주제 (2)_start→main→parse_options 전체 호출 체인 ASCII (3)타 모듈 20+종 카탈로그 (4)주요 함수·구조체 요약 (5)"왜 복잡한가" 섹션 포함으로 전면 재작성; #include 17종 사유 전수 인라인(stdio FILE/fopen/fgets/sprintf·stdlib malloc/exit·unistd getpid/access·ctype isspace/iscntrl·string strsep/strdup·errno EINVAL/ENOMEM/ENOSPC·sys/ipc IPC_CREAT/RMID·sys/types pid_t·dlfcn dlopen/dlsym·valgrind/drd DRD_IGNORE_VAR·fio.h 중앙 심볼 카탈로그·sys/shm shmget/at/dt/ctl·parse.h fio_options_parse·smalloc sinit·filehash file_hash_exit·verify verify_header·profile·server·idletime·filelock·steadystate·blktrace·oslib asprintf/getopt/strcasestr·crc/test·lib/pow2·lib/memcpy); 전역 변수 §4 멀티라인 전수 재작성(fio_version_string/FIO_RANDSEED/ini_file/dump_cmdline/parse_only/merge_blktrace_only/def_thread/segments[]/job_sections/nr_job_sections/exitall_on_terminate/output_format/eta_print/eta_interval_msec/eta_new_line/f_out/f_err/exec_profile/warnings_fatal/terse_version/is_backend/is_local_backend/nr_clients/log_syslog/write_bw_log/write_bw_log_name/read_only/status_interval/trigger_*/aux_path/prev_group_jobs/fio_debug/fio_debug_jobno/fio_debug_jobp/fio_warned/cmd_optstr/did_arg/FIO_CLIENT_FLAG — 각각 설정자/읽는 자/값범위/동기화 4요소); l_opts[] 테이블 공통 규약 블록(struct option 필드 의미·FIO_CLIENT_FLAG 비트 규약·NULL 센티넬); 주요 함수 §2 전면 재작성(free_threads_shm/free_shm/add_thread_segment/expand_thread_area/dump_print_option/dump_opt_list/copy_opt_list/get_new_job/put_job/__setup_rate/setup_rate/fixed_block_size/gcd/fixup_options/init_rand_file_service/init_rand_offset_seed/td_fill_rand_seeds/setup_random_seeds/ioengine_load/init_flags/make_filename/parse_dryrun/gen_log_name/check_waitees/wait_for_ok/verify_per_group_options/make_log_name/add_job/add_job_opts/skip_this_section/is_empty_or_comment/__parse_jobs_ini/parse_jobs_ini/fill_def_thread/show_debug_categories/usage/set_debug/fio_options_fill_optstring/client_flag_set/parse_cmd_client/show_closest_option/parse_output_format/parse_cmd_line/fio_init_options/parse_options/options_default_fill/get_global_options — @param/@return/동작 단계/실행 컨텍스트/호출 체인/에러 경로 구성); enum FPRE_*/struct fpre_keyword §4 멀티라인; debug_levels[] 정적 테이블 §4. 남은 작업: 함수 내부 실행 라인(inline) 세부 보강은 대부분 기존 인라인이 이미 적절하여 유지했으며, 4000+ 라인 fixup_options 200+ 검증 지점의 모든 개별 라인에 세부 인라인을 붙이는 작업은 후속 세션에서 가능 | 1093 → 1652 (4364 → 5100 라인) |
| ioengines.c | 신기준 | ● | 플러그인 계약 허브: register/unregister/load_ioengine/dlopen_ioengine/check_engine_ops, td_io_init/prep/queue/commit/getevents 디스패치, FIO_Q_COMPLETED/QUEUED/BUSY 반환 의미, ioengine_ops 14개 플래그 비트 풀이(SYNCIO/DISKLESSIO/NOIO/RAWIO/MEMALIGN/PIPEIO/BARRIER/UNIDIR/NOEXTEND/NODISKUTIL/ASYNCIO_SYNC_TRIM/ASYNCIO_SETS_ISSUE_TIME/RO/MULTI_RANGE_TRIM/NO_OFFLOAD), io_u_mark_submit/complete/depth 계측 훅 시점, offload overlap_check 뮤텍스. 2026-04-15 보강 |
| stat.c | 908/5131 (18%) | ◐ | 2026-04-18 대폭 보강: 상단 4섹션 전면 재작성(샘플 수집 ASCII 다이어그램·퍼센타일 히스토그램 로그 스케일 수학·4가지 출력 포맷 매트릭스·thread_stat 구조 핵심·주의사항 추가 섹션). #include 17종 사유 전수 인라인. struct log_sample 6필드 §4 멀티라인. stat_sem §4. 전 70+ 함수 §2 헤더 추가(@param/@return/호출 체인/실행 컨텍스트/동작 단계/에러 경로): clear_rusage_stat/update_rusage_stat/plat_val_to_idx/plat_idx_to_val(수학적 배경 포함)/double_cmp/calc_clat_percentiles/show_clat_percentiles/get_nr_prios_with_samples/calc_lat(Welford 알고리즘 명시)/show_mixed_group_stats/show_group_stats/stat_calc_dist/stat_calc_lat/stat_calc_lat_nu/stat_calc_lat_n/u/m/display_lat/gen_mixed_ddir_stats_from_ts/convert_agg_kbytes_percent/show_ddir_status/show_mixed_ddir_status/show_lat/show_lat_n/u/m/show_latencies/block_state_category/compare_block_infos/calc_block_percentiles/show_block_infos/show_ss_normal/show_agg_stats/aggregate_slaves_stats/print_disk_util/json_array_add_disk_util/json_object_add_disk_utils/show_disk_util/scale_down_ns/show_thread_status_normal(출력 13단계 명시)/show_ddir_status_terse(terse CSV 필드 순서)/show_mixed_ddir_status_terse/add_ddir_lat_json/add_ddir_status_json/add_mixed_ddir_status_json/show_thread_status_terse_all/json_add_job_opts/show_thread_status_json(JSON 구조 명시)/show_thread_status_terse/show_thread_status(디스패처)/__sum_stat(Chan 병렬 합성 공식)/sum_stat/sum_group_stats/free_clat_prio_stats/alloc_clat_prio_stat_ddir/grow_clat_prio_stat/find_clat_prio_index/alloc_or_get_clat_prio_index/clat_prio_stats_copy/clat_prio_stat_add_samples/sum_clat_prio_stats_src_single_prio/sum_clat_prio_stats_src_multi_prio/sum_clat_prio_stats/sum_thread_stats(MIXED 모드 설명)/init_group_run_stat/init_thread_stat_min_vals/init_thread_stat/init_per_prio_stats/__show_run_stats(8단계 처리 흐름 ASCII)/__show_running_run_stats/check_status_file/check_for_running_stats/add_stat_sample(Welford 수식 전개)/add_stat_prio_sample/get_new_log/regrow_log/regrow_logs/regrow_agg_logs/get_cur_log/__add_log_sample/reset_io_stat/reset_io_u_plat/reset_clat_prio_stats/reset_io_stats/__add_stat_to_log/_add_stat_to_log/add_log_sample(평균 윈도우/즉시 모드 분기)/finalize_logs/add_agg_sample/add_sync_clat_sample/add_lat_percentile_sample/add_lat_percentile_prio_sample/add_clat_sample(5 경로 분배)/add_slat_sample/add_lat_sample/add_bw_sample/__add_samples/add_bw_samples/add_iops_sample/add_iops_samples/td_in_logging_state/calc_log_samples/stat_init/stat_exit/show_running_run_stats/io_u_block_info. 핵심 함수(plat_val_to_idx/calc_clat_percentiles/add_stat_sample/__sum_stat/add_clat_sample)에 수학적 배경 + 실행 라인 인라인 보강. **미완료**: 대형 함수(show_ddir_status/show_thread_status_normal/show_thread_status_json/__show_run_stats/sum_thread_stats 등) 의 본문 수십~100+ 라인에 대한 §3 전수 인라인은 상단 함수 §2 헤더로 요약만 제공 (기존 얕은 인라인 유지). 2026-04-18 보강 | 111 → 908 (3929 → 5131 라인)
| iolog.c | 1139/3209 (35%) | ● | 2026-04-18 신기준 마감. 기존 ◐(239/2175, 11% — 얕은 한 줄 주석)에서 상단 4섹션을 두 축(write iolog / read iolog / 성능 로그 / 압축 / blktrace / 보조 기능 6개 대주제) + write·read·성능로그·잡종료 4개 호출 체인 ASCII + iolog 텍스트 파일 포맷 스펙(v2/v3 I/O·파일 라인 포맷 + sscanf 필드 수 매핑 io_act/file_act) + 성능 로그 파일 포맷(.log plain/BOTH/hist/.log.gz) + 주요 함수·구조체 요약 30+개로 전면 재작성; #include 15종 사유 전수 인라인(stdio/stdlib/assert/sys/types/sys/stat/unistd/zlib/flist/fio.h 60+심볼 카탈로그/trim/filelock/smalloc/blktrace/pshared/lib/roundup + 네트워크 6종); 전방 선언 2종(iolog_flush/read_iolog) 사유 설명; 전역 iolog_ver2/ver3 §4(설정자·읽는 자·동기화); io_act/file_act 매크로 §4 풀이(v2/v3 포맷 차이); struct iolog_flush_data 5필드(work/log/samples/nr_samples/free)·struct inflate_chunk_iter 6필드(seq/err/buf/buf_size/buf_used/chunk_sz) §4 멀티라인; 전 45+ 함수 §2(queue_io_piece/log_io_u/log_file/iolog_delay/ipo_special/delay_since_ttime/read_iolog_get/prune_io_piece_log/log_io_piece/unlog_io_piece/trim_io_piece/write_iolog_close/iolog_items_to_fetch/read_iolog/is_socket/open_socket/init_iolog_read/init_iolog_write/init_iolog/setup_log/set_file_buffer/clear_file_buffer/free_log/hist_sum/flush_hist_samples/print_sample_fields/flush_samples/get_new_chunk/free_chunk/z_stream_init/finish_chunk/inflate_chunk/inflate_gz_chunks/iolog_file_inflate/flush_log/finish_log/log_chunk_sizes/iolog_put_deferred/iolog_free_deferred/gz_work/gz_work_async/gz_init_worker/iolog_compress_init/iolog_compress_exit/iolog_flush/iolog_cur_flush/iolog_cur_log/iolog_nr_samples/__write_log/write_iops_log/write_slat_log/write_clat_log/write_clat_hist_log/write_lat_log/write_bandw_log/td_writeout_logs/fio_writeout_logs) 각각 @param/@return/동작 단계/실행 컨텍스트/호출 체인/에러 경로 포함; 모든 실행 라인 인라인 — FIFO io_log_list append + total_io_size 누적, RB 삽입 루프(파일/오프셋 정렬 + overlap 감지 goto restart + IP_F_IN_FLIGHT 보호), IP_F_ONRB/ONLIST 플래그 판별, replay_time_scale 스케일 공식 (100/scale), sscanf 포맷 분기 v2("%256s %256s %llu %u") vs v3("%llu %256s %256s %llu %u"), io_act/file_act 매크로 필드 수 매핑, replay_redirect/replay_skip/replay_scale/replay_align 4옵션 효과, DDIR_INVAL 특수 동작 마커 + FIO_LOG_OPEN/CLOSE/UNLINK/ADD_FILE 전수 분기, BLOCK_STATE_TRIM/WRITE_FAILURE 전이 단조성, AF_UNIX SOCK_STREAM + sun_path 108B 제한 + connect addrlen = strlen+sizeof(sun_family), "fio version 3 iolog" 매직 검증 v1 거부, log_ddir_mask 4비트(LOG_OFFSET/PRIO/AVG_MAX/ISSUE_TIME_SAMPLE_BIT) 설정 근거, scalloc SHM 힙 공유(집계 로그 fork 자식 접근), roundup_pow2 iodepth 정렬 캐시 친화, mutex_init_pshared PTHREAD_PROCESS_SHARED 잡-압축 스레드 동기화, TD_F_COMPRESS_LOG 트리거 분기, hist_sum delta vs absolute 계산, flush_samples 조건부 필드 조립(offset/prio/avg_max/issue_time + 256B 스택 버퍼 vsnprintf 누적), zlib deflate Z_NO_FLUSH/Z_FINISH/Z_BUF_ERROR/Z_STREAM_END 프로토콜 분기, inflateInit2 wbits=15+32 gzip 자동 감지, GZ_CHUNK=128 KiB 선정 근거, iolog_put_deferred UAF 방지 지연 해제 큐, workqueue gz_work_async 비동기 + chunk_lock splice_tail 일괄 병합, log_gz_cpumask 어피니티 via fio_setaffinity/sched_setaffinity, iolog_cur_flush 소유권 이전(cur_log 리셋 + deferred_free 정리), iolog_flush 동기 블로킹 전체 압축, log_gz_store 바이너리 .log.gz vs 텍스트 .log 분기, fopen "wb"/"ab" 바이너리 모드 Windows 라인엔딩 회피, set_file_buffer 1 MiB setvbuf _IOFBF 대용량 로그 속도, fio_trylock_file/lock_file per_job_logs 충돌 회피, log_types[] 비트마스크 매핑 테이블 + 5ms usleep 재시도 루프 + log_left>1 trylock vs 마지막 블로킹 락, for_each_td 전체 잡 순회, TD_FINISHING runstate 전이. 상단 블록은 독자가 다른 파일로 점프하지 않고도 두 축 흐름·호출 체인·포맷·실행 컨텍스트·공유 자료구조를 이해할 수 있도록 구성. 본 파일은 backend/ioengines/io_u/stat/blktrace/workqueue/filelock/smalloc/pshared과의 연결을 상세 섹션으로 명시. | 239 → 1139 (2175 → 3209 라인) |
| verify.c | 1348/3487 (39%) | ● | 2026-04-18 신기준 마감. 상단 4섹션 대폭 확장(18종 verify type 카탈로그 + 쓰기/동기 검증/오프로드 검증 3개 경로 호출 체인 ASCII + 검증 블록 바이트 레이아웃 + 공유 상태 섹션 + 주요 함수 30+ 요약); #include 11종 사유 전수 인라인(unistd/fcntl/string/assert/pthread/libgen/arch.h/fio.h/verify.h/trim.h/lib/rand·hweight·pattern/oslib/asprintf + crc/md5·crc64·crc32·crc32c·crc16·crc7·sha256·sha512·sha1·xxhash·sha3); 전 60+ 함수 §2(populate_verify_io_u/verify_io_u/verify_io_u_async/get_next_verify/verify_async_thread/init/exit/fio_verify_init/verify_header/populate_hdr/__fill_hdr/fill_hdr/fill_xxhash/fill_sha3 공용+224/256/384/512/fill_sha512/sha256/sha1/crc7/crc16/crc32/crc32c/crc64/md5/verify_io_u_xxhash/sha3 공용+4종/sha512/sha256/sha1/crc7/crc16/crc32/crc32c/crc64/md5/verify_io_u_pattern/check_pattern/pattern_need_buffer/io_u_verify_off/log_verify_failure/hexdump/dump_buf/__dump_verify_buffers/dump_verify_buffers/verify_trimmed_io_u/mem_is_zero/mem_is_zero_slow/fill_buffer_pattern/__fill_buffer/fill_verify_pattern/fill_pattern_headers/get_hdr_inc/__hdr_size/hdr_size/hdr_priv/memswp/paste_blockoff/get_all_io_list/open_state_file/write_thread_list_state/__verify_save_state/verify_save_state/verify_free_state/verify_assign_state/verify_state_hdr/verify_load_state/verify_state_should_stop); struct vcont 7필드 §4 멀티라인(io_u/hdr_num/td/name/good_crc/bad_crc/crc_len 입력·출력 분리 설명); 모든 실행 라인 인라인(FIO_HDR_MAGIC 0xacca·VERIFY_HEADER_VERSION 0x81 최상위 비트·block layout 바이트 슬롯·memswp 스왑 메커니즘·CRC32C offsetof 자기 제외·hweight8 비트 에러 진단·Rusty Russell "memcmp with self"·asprintf aux_path elvis·fio_did_warn 재발 방지·atomic_load_acquire IP_F_IN_FLIGHT·pthread_cond broadcast/signal+detach·read_barrier+exit 플래그·flist_splice_init 락 최소화·O_SYNC 크래시 안전·LE 직렬화 cpu_to_le/le_to_cpu·thread_io_list_sz 가변·io_list_next 포인터 산술·RNG use64 32b/64b Tausworthe·verify_state_should_stop 재시작 (a)/(b) 판정·__fill_random_buf_percentage compress_chunk·paste_format specifier %o↔paste_blockoff·typecheck·hdr_priv 포인터 산술·verify_offset memswp 대칭·numberio 3조건 AND·IO_U_F_VER_IN_DEV HW PI 우회·FIO_FAKEIO null 엔진 우회·verify_fatal→fio_mark_td_terminate·verify_write_sequence opt-in·SSE4.2/ARMv8 CRC32C HW probe); 18종 verify type 전수 커버(NONE/HDR_ONLY/NULL/MD5/CRC64/CRC32/CRC32C/CRC32C_INTEL/CRC16/CRC7/SHA256/SHA512/SHA3_224/256/384/512/XXHASH/SHA1/PATTERN/PATTERN_NO_HDR). 300 → 1348 (2281 → 3487 라인) |
| options.c | 1382/8311 (16.6%) | ◐ | 2026-04-18 2차 세션 §2 콜백 대규모 보강. 상단 4섹션+공통 규약 블록(fio_option 필드 핸드북 포함)+fio_keywords struct §4 는 이전 세션 기완비. 본 세션에 str_*_cb 콜백 약 30+ §2 승격(파라미터·반환·왜 필요한가·동작 단계·호출 체인·읽는 자·실행 컨텍스트): str_replay_skip_cb / ignore_error_cb / rw_cb / mem_cb / fio_clock_source_cb / rwmix_read_cb / rwmix_write_cb / exitall_cb / cpumask_cb / cpus_allowed_cb / verify_cpus_allowed_cb / log_cpus_allowed_cb / numa_cpunodes_cb / numa_mpol_cb / fst_cb / sfr_cb / random_distribution_cb / steadystate_cb / filename_cb / directory_cb / opendir_cb / buffer_pattern_cb / buffer_compress_cb / dedupe_cb / verify_pattern_cb / gtod_reduce_cb / offset_cb / offset_increment_cb / size_cb / io_size_cb / zoneskip_cb / write_bw_log_cb / write_lat_log_cb / write_iops_log_cb / write_hist_log_cb / ioengine_external_cb / rw_verify / gtod_cpu_verify / fdp_pli_cb / dp_scheme_cb / bssplit_cb / parse_cmdprio_bssplit_entry / split_parse_prio_ddir / bssplit_ddir / zone_split_ddir / parse_zoned_distribution / ignore_error_type / is_valid_steadystate / get_next_str / get_max_str_idx / set_name_idx / get_name_by_idx / fio_fdp_cmp / fio_split_prio_cmp / fio_cpus_split / set_cpus_allowed / str_split_parse / str2error / add_to_lopt / options_to_lopts / fio_options_set_ioengine_opts / fio_options_dup_and_init / fio_keywords_exit / fio_keywords_init / bc_calc / fio_option_dup_subs / fio_keyword_replace / dup_and_sub_options / show_closest_option / fio_options_parse / fio_cmd_option_parse / fio_cmd_ioengine_option_parse / fio_fill_default_options / fio_show_option_help / fio_options_mem_dupe / fio_get_kb_base / add_option / invalidate_profile_options / add_opt_posval / del_opt_posval / fio_options_free / fio_dump_options_free / fio_option_find / find_next_opt / opt_is_set / __fio_option_is_set / fio_option_mark_set. 주요 함수에 호출 체인/파라미터 3-way 인코딩(offset/size/io_size/zoneskip의 바이트/퍼센트/존 마커)/5단계 파싱 드라이버/FDP NVMe TP 4146 맥락/SigV4 체인/bc 보안 맥락 삽입. **미완료**: fio_options[] 약 300여 엔트리 개별 인라인(공통 규약 블록으로 대부분 커버되나 complex 엔트리 수십 개는 개별 보강 여지). 한국어 521→1382, 라인 6957→8311. |
| server.c | 1387/4669 (29.7%) | ◐ | 2026-04-18 2차 보강. 상단 4섹션 기완비 유지. 본 세션에 (a) **handle_command opcode dispatch switch 12 case 전수 주석** (QUIT/EXIT/LOAD_FILE/JOB/JOBLINE/PROBE/SEND_ETA/RUN/UPDATE_JOB/VTRIGGER/SENDFILE/default) (b) 80+ 함수 §2 헤더 승격 — sk_lock/unlock/sk_out_assign/free/drop/__fio_init_net_cmd/fio_init_net_cmd/fio_server_op/iov_total_len/fio_sendv_data/fio_send_data/fio_server_poll_fd/fio_recv_data/verify_convert_cmd/fio_net_recv_cmd(프래그먼트 재조립)/add_reply/alloc_reply/free_reply/fio_net_cmd_crc_pdu/fio_net_cmd_crc/fio_net_send_cmd/fio_net_prep_cmd/fio_net_queue_entry/fio_net_queue_cmd/fio_net_send_simple_stack_cmd/fio_net_send_simple_cmd/fio_net_queue_quit/fio_net_send_quit/fio_net_send_ack/fio_net_queue_stop/fio_server_add_fork_item(POSIX+Win)/fio_server_add_conn_pid/fio_server_add_job_pid/fio_server_check_fork_item(POSIX waitpid WIFSIGNALED/WIFEXITED/ECHILD + Win pthread_kill/GetExitCodeProcess)/fio_server_fork_item_done/fio_server_check_fork_items/jobs/conns/handle_load_file_cmd/fio_backend_thread(Win)/handle_run_cmd(POSIX fork/Win pthread)/handle_job_cmd/handle_jobline_cmd/handle_probe_cmd/handle_send_eta_cmd/send_update_job_reply/handle_update_job_cmd/handle_trigger_cmd/fio_send_cmd_ext_pdu(writev iov[2])/finish_entry/entry_set_flags/send_vec_entry/handle_sk_entry/handle_xmits(flist_splice_init)/handle_connection/get_my_addr_str/handle_connection_process(Win pipe+WSASocket)/accept_loop/fio_server_text_output/convert_io_stat(IEEE754 보존)/convert_gs/fio_server_send_ts(extended_buf + clat_prio + ss 링버퍼)/fio_server_send_gs/fio_server_send_job_options/convert_agg/convert_dus/fio_server_send_du/__fio_net_prep_tail/__deflate_pdu_buffer/__fio_append_iolog_gz_hist(delta 계산)/__fio_append_iolog_gz/fio_append_iolog_gz/fio_append_gz_chunks(chunk_lock)/fio_append_text_log/fio_send_iolog(STORE_COMPRESSED/XMIT_COMPRESSED/평문 3분기)/fio_server_send_add_job/fio_server_send_start/fio_server_get_verify_state(세마포어 동기 + 10s 타임아웃)/fio_init_server_ip(SO_REUSEADDR/REUSEPORT)/fio_init_server_sock(umask 000)/fio_init_server_connection(listen backlog=4)/fio_server_parse_host(inet_pton→getaddrinfo 폴백)/fio_server_parse_string(sock:/ip:/ip6:/host,port 형식)/fio_handle_server_arg/sig_int(async-signal-safe unlink)/set_sig_handlers(SA_RESTART)/fio_server_destroy_sk_key/fio_server_create_sk_key/fio_server/fio_server_got_signal(SIGPIPE 분기)/check_existing_pidfile(SIGCONT 생존 확인)/write_pid/fio_start_server(setsid+openlog+freopen /dev/null)/fio_server_set_arg/fio_server_internal_set. (c) 전반적 인라인 보강 — writev/recv/accept/bind/listen/socket/setsockopt/SO_REUSEADDR/REUSEPORT/SA_RESTART/MSG_WAITALL/MSG_DONTWAIT/SIGINT/SIGPIPE/SIGCONT/SIGBREAK/fork/waitpid(WNOHANG)/ECHILD/pthread_create/pthread_kill/pthread_join/setsid/openlog/freopen /dev/null/unlink/FIO_NET_CMD_F_MORE/FIO_SERVER_VER/FIO_SERVER_MAX_FRAGMENT_PDU/cpu_to_le16/32/64/le*_to_cpu/fio_crc16/FIO_NET_CMD_CRC_SZ/fio_double_to_uint64/IEEE754/EAGAIN/EINTR/ECONNRESET/EPIPE/deflate Z_BLOCK·Z_FINISH·Z_STREAM_END/deflateInit·deflateEnd/STORE_COMPRESSED/XMIT_COMPRESSED/SK_F_INLINE·VEC·SIMPLE·FREE·COPY 전수 주석화. **미완료**: struct fio_net_cmd/sk_out/fio_net_cmd_reply §4 멀티라인은 server.h 소속이라 본 파일 범위 외; fio_server_send_ts/fio_net_recv_cmd 일부 긴 루프의 세부 라인 인라인 잔여. | 546 → 1387 (3679 → 4669 라인) |

#### 그 외 디렉토리 요약

| 디렉토리 | 파일 수 | 일괄 상태 | 비고 |
|---------|--------:|:---:|------|
| lib/ | 45 | ◐ | 구기준 완료, 인라인 보강 필요 |
| arch/ | 18 | ◐ | 구기준 완료 |
| t/ | 19 | ◐ | |
| unittests/ | 10 | ◐ | |
| profiles/ | 2 | ◐ | |
| mock-tests/ | 2 | ◐ | |
| compiler/ | 1 | ◐ | |
| exp/ | 1 | ◐ | |
| os/ | 23 | ○/◐ | 구기준 미완료 — 헤더 일부만 얕은 주석 |
| crc/ | 33 | ○/◐ | 구기준 미완료 |
| oslib/ | 26 | ○/◐ | 구기준 미완료 |

### 작업 로그

각 세션에서 신기준으로 보강한 파일을 **맨 위에** 추가한다. 형식: `- YYYY-MM-DD | <경로> | <요약> | 한국어 라인 <before>→<after>`.

- 2026-04-18 | server.c | fio 서버 프로토콜 엔진(3679 라인) 2차 보강(◐ 유지, 29.7%, 후속 1회 패스로 ● 승격 가능). 이전 세션에 상단 4섹션·구조체 §4(sk_entry/fio_fork_item/cmd_reply/ffi_element)·SK_F_* enum §4·전역변수 §4·opcode 문자열 테이블은 완비되어 있었음. 본 세션 핵심 추가: **handle_command 12-way opcode dispatch switch 전수 case 주석** (QUIT/EXIT/LOAD_FILE/JOB/JOBLINE/PROBE/SEND_ETA/RUN/UPDATE_JOB/VTRIGGER/SENDFILE/default — 각 case 의 의미·호출 함수·반환 규약·fork 경로), **80+ 함수 §2 전면 재작성** (저수준 sk_lock/unlock/sk_out 생명주기 + __fio_init_net_cmd/fio_init_net_cmd/verify_convert_cmd(CRC16+LE→CPU+FIO_SERVER_VER+pdu_len)/fio_net_recv_cmd(F_MORE 프래그먼트 재조립+TEXT/JOB NUL 종결)/fio_sendv_data(writev+EAGAIN·EINTR·exit_backend)/fio_recv_data(MSG_WAITALL vs MSG_DONTWAIT)/fio_net_send_cmd/fio_net_prep_cmd/fio_net_queue_entry(INLINE 분기)/fio_net_send_simple_cmd/alloc_reply·add_reply·free_reply(포인터 비트 캐스트) 및 포크 추적 POSIX+Windows 양쪽 각각(fio_server_add_fork_item·conn_pid·job_pid·check_fork_item waitpid WIFSIGNALED·WIFEXITED·ECHILD + pthread_kill·join + GetExitCodeProcess STILL_ACTIVE)·fio_server_fork_item_done·check_fork_items·check_jobs/conns), opcode 핸들러 전수(handle_load_file_cmd parse_jobs_ini is_buf=0/handle_run_cmd POSIX fork+자식 _exit vs Win pthread_create·refs 2 증가/handle_job_cmd is_buf=1/handle_jobline_cmd argv 재구성/handle_probe_cmd zlib 네고·me[]/handle_send_eta_cmd 빈 응답 폴백/handle_update_job_cmd ENODEV/handle_trigger_cmd exec_trigger), 전송 계층(fio_send_cmd_ext_pdu writev iov[2]/finish_entry SK_F_FREE·COPY 분기/send_vec_entry/handle_sk_entry xmit 세마포어 직렬화/handle_xmits flist_splice_init 락 최소화), 연결 루프(handle_connection poll+recv+dispatch+_exit + POLLIN/POLLERR/POLLHUP 해석/accept_loop fork vs Win CreateProcess/handle_connection_process Win 명명된 파이프+WSASocket/get_my_addr_str getsockname), 결과 스트리밍(fio_server_text_output TLS 조회/convert_io_stat IEEE754 보존/convert_gs DDIR_RWDIR_CNT/fio_server_send_ts cmd_ts_pdu + extended_buf(clat_prio + ss 링버퍼)/fio_server_send_gs/send_job_options truncated/convert_agg·dus/fio_server_send_du/send_add_job thread_options_pack), 압축(__fio_net_prep_tail/__deflate_pdu_buffer Z_BLOCK/__fio_append_iolog_gz_hist delta/fio_append_iolog_gz deflateInit+Z_FINISH+deflateEnd/fio_append_gz_chunks chunk_lock/fio_append_text_log/fio_send_iolog STORE_COMPRESSED·XMIT_COMPRESSED·평문 3분기), verify state 동기(fio_server_get_verify_state 세마포어+10s 타임아웃+verify_state_hdr 검증), 초기화(fio_init_server_ip SO_REUSEADDR+REUSEPORT/fio_init_server_sock AF_UNIX+umask 000/fio_init_server_connection listen backlog=4/fio_server_parse_host inet_pton→getaddrinfo 폴백/fio_server_parse_string sock:/ip:/ip6:/host,port 5형식), 시그널/데몬(sig_int async-signal-safe unlink/set_sig_handlers SA_RESTART/fio_server_got_signal SIGPIPE sk=-1 vs 기타 exit_backend/check_existing_pidfile SIGCONT/write_pid/fio_start_server setsid+openlog+freopen /dev/null), TLS(sk_key create/destroy), 외부 API(fio_server_set_arg/internal_set Win 파이프 이름). 인라인 전수 보강: writev/recv/accept/bind/listen/socket/setsockopt/SO_REUSEADDR/REUSEPORT/SA_RESTART/MSG_WAITALL/MSG_DONTWAIT/SIGINT/SIGPIPE/SIGCONT/SIGBREAK/fork/waitpid(WNOHANG)/ECHILD/pthread_create·kill·join/setsid/openlog/freopen /dev/null/unlink/FIO_NET_CMD_F_MORE/FIO_SERVER_VER/FIO_SERVER_MAX_FRAGMENT_PDU/cpu_to_le16·32·64/le*_to_cpu/fio_crc16/FIO_NET_CMD_CRC_SZ/fio_double_to_uint64 IEEE754/EAGAIN/EINTR/ECONNRESET/EPIPE/deflate Z_BLOCK·Z_FINISH·Z_STREAM_END/deflateInit·deflateEnd/STORE_COMPRESSED·XMIT_COMPRESSED/SK_F_INLINE·VEC·SIMPLE·FREE·COPY 전수 주석화. 미완: struct fio_net_cmd/sk_out/fio_net_cmd_reply §4 (server.h 소속이라 본 파일 범위 외), fio_server_send_ts/fio_net_recv_cmd 일부 대형 변환 루프의 세부 라인 인라인 잔여. | 546 → 1387 (3679 → 4669 라인)
- 2026-04-18 | options.c (2차) | fio 옵션 정의·파싱 허브(8311 라인) 2차 §2 대규모 보강. 상단 4섹션+fio_option 필드 핸드북 공통 규약 블록은 이전 세션 기완비(보존). 본 세션에 str_*_cb 콜백 약 30+ (replay_skip/ignore_error/rw/mem/fio_clock_source/rwmix_read·write/exitall/cpumask/cpus_allowed/verify_cpus_allowed/log_cpus_allowed/numa_cpunodes/numa_mpol/fst/sfr/random_distribution/steadystate/filename/directory/opendir/buffer_pattern/buffer_compress/dedupe/verify_pattern/gtod_reduce/offset/offset_increment/size/io_size/zoneskip/write_bw_log/write_lat_log/write_iops_log/write_hist_log/ioengine_external) + .verify 콜백 2종(rw_verify/gtod_cpu_verify) + FDP 콜백 2종(str_fdp_pli_cb/str_dp_scheme_cb) + bssplit 계열 6종(bssplit_cb/parse_cmdprio_bssplit_entry/split_parse_prio_ddir/bssplit_ddir/zone_split_ddir/parse_zoned_distribution) + 보조 함수 20+ (ignore_error_type/is_valid_steadystate/get_next_str/get_max_str_idx/set_name_idx/get_name_by_idx/fio_fdp_cmp/fio_split_prio_cmp/fio_cpus_split/set_cpus_allowed/str_split_parse/str2error/add_to_lopt/options_to_lopts) + 외부 API 전수(fio_options_set_ioengine_opts/fio_options_dup_and_init/fio_keywords_exit/fio_keywords_init/bc_calc/fio_option_dup_subs/fio_keyword_replace/dup_and_sub_options/show_closest_option/fio_options_parse/fio_cmd_option_parse/fio_cmd_ioengine_option_parse/fio_fill_default_options/fio_show_option_help/fio_options_mem_dupe/fio_get_kb_base/add_option/invalidate_profile_options/add_opt_posval/del_opt_posval/fio_options_free/fio_dump_options_free/fio_option_find/find_next_opt/opt_is_set/__fio_option_is_set/fio_option_mark_set) 전수 §2 신기준 헤더(@param/@return/왜 필요한가/동작 단계/호출 체인/읽는 자/실행 컨텍스트 포함)로 승격. 핵심 추가 맥락: 3-way 인코딩(offset/size/io_size/zoneskip의 바이트 vs 퍼센트 vs 존 단위 parse_is_*/ZONE_BASE_VAL), 5단계 fio_options_parse 드라이버(sort→sub→parse→engine fallback→closest), NVMe FDP TP 4146 Placement ID / RUH 매핑, cmdprio_bssplit의 ioprio_value(class/level/hint) 16비트 인코딩, bc_calc popen 보안 주의, "external:/path/to/so" 메모리 레이아웃, OPT_MAGIC 검증 해킹, fio_keywords_init 의 $pagesize/$mb_memory/$ncpus 런타임 계산. struct fio_keyword 3필드 §4 멀티라인 재작성. 미완: fio_options[] 약 300여 엔트리의 개별 인라인은 공통 규약 블록으로 대부분 커버되나 complex 엔트리 수십 개(parent/.cb/.posval 겹친 것) 는 추가 보강 여지. ◐ 유지 — 다음 세션에서 fio_options[] 엔트리 후반부 및 rate/latency/verify 옵션 그룹 개별 인라인 1회 패스 후 ● 승격 가능. | 521 → 1382 (6957 → 8311 라인)
- 2026-04-18 | stat.c | fio 통계 수집·계산·출력 엔진(3929→5131 라인) 상단 4섹션/§2 함수 헤더 대규모 보강. 상단 블록을 샘플 수집 경로 ASCII 다이어그램·퍼센타일 히스토그램 로그 스케일 수학 설명·4가지 출력 포맷 매트릭스(Normal/Terse v2-v5/JSON/JSON+hist)·thread_stat 구조 핵심·Welford 알고리즘 수식·주의사항 추가 섹션으로 전면 재작성. #include 17종 사유 전수 인라인(stdio/string/stdlib qsort/sys/time rusage 변환/sys/stat check_status_file/math sqrt·log10/fio.h thread_data·thread_stat·io_stat/diskutil.h/lib/ieee754 fio_fp64_t/json.h/lib/getrusage/idletime.h/lib/pow2 is_power_of_2/lib/output_buffer buf_output/helper_thread.h/smalloc sfree/zbd.h/oslib/asprintf). LOG_MSEC_SLACK 매크로 OS 분기 근거 인라인. struct log_sample 6필드(data/ddir/bs/offset/priority/issue_time) §4 멀티라인. stat_sem §4. 전 70+ 함수 §2 헤더 전수 추가(@param/@return/호출 체인/실행 컨텍스트/동작 단계/에러 경로). 핵심 함수별 수학적 배경: plat_val_to_idx(로그 스케일 버킷 MSB/error_bits 계산, PLAT_BITS=6 → 1/64 오차), plat_idx_to_val(버킷 중앙값 계산), calc_clat_percentiles(누적 합 알고리즘 O(PLAT_NR+len)), calc_lat(Welford stddev=sqrt(S/(n-1))), __sum_stat(Chan 1979 병렬 합성 공식 M2 보정), add_stat_sample(Welford 온라인 전체 수식), add_clat_sample(5 경로 분배 — clat_stat/prio/iolog/histogram/hist_log), sum_thread_stats(MIXED UNIFIED_MIXED 경로 분기). __show_run_stats 8단계 처리 흐름 ASCII 삽입. show_thread_status_normal 13항목 출력 구성 명시. show_ddir_status_terse 에 v2/v3/v4/v5 CSV 필드 레이아웃 기재. show_thread_status_json JSON 중첩 구조 문서화. add_log_sample 평균 윈도우/즉시 모드 분기 설명. calc_log_samples 헬퍼 스레드 루프 흐름 명시. 일부 핵심 함수(calc_clat_percentiles/add_stat_sample/plat_val_to_idx/__sum_stat/clear_rusage_stat/update_rusage_stat) 본문 실행 라인 인라인 보강 완료. 미완: 대형 출력 함수(show_ddir_status/show_thread_status_normal/show_thread_status_json/__show_run_stats/sum_thread_stats 등) 본문 수십~100+ 라인의 §3 전수 인라인은 함수 §2 헤더로 요약만 제공. ◐ 유지. 현재 18% 비율. | 111 → 908 (3929 → 5131 라인)
- 2026-04-18 | verify.c | fio 데이터 무결성 검증 엔진(2281 라인) 신기준 마감. 기존 ◐(300/2281 = 13%, 얕은 한 줄 주석·상단 블록은 있으나 §4 필드 멀티라인 미달·일부 함수만 §2·인라인 드묾)에서 본 세션에 (a) 상단 4섹션 대폭 확장 — 18종 verify type 카탈로그(NONE/HDR_ONLY/NULL/MD5/CRC64/CRC32/CRC32C/CRC32C_INTEL/CRC16/CRC7/SHA1/SHA256/SHA512/SHA3_224/256/384/512/XXHASH/PATTERN/PATTERN_NO_HDR)·쓰기/동기 검증/오프로드 검증 3개 호출 체인 ASCII·검증 블록 바이트 레이아웃 ASCII([magic|ver|ty|len|rand_seed|offset|time_sec|time_nsec|numberio|thread|pad|crc32|vhdr_*|body])·공유 상태 6종(td->verify_list/verify_cond/verify_thread_exit/nr_verify_threads/io_hist_tree/io_hist_list/verify_state/vstate)·주요 함수 30+ 요약; (b) #include 11종 사유 전수 인라인(unistd/fcntl/string/assert/pthread/libgen/arch.h/fio.h 중앙 심볼 카탈로그/verify.h/trim.h/lib/rand·hweight·pattern/oslib/asprintf + 체크섬 11헤더 md5/crc64/crc32/crc32c/crc16/crc7/sha256/sha512/sha1/xxhash/sha3 각 알고리즘 특성); (c) 전방 선언 2개(populate_hdr/__fill_hdr) 사유 설명; (d) 전 60+ 함수 §2 전면 재작성(populate_verify_io_u/verify_io_u/verify_io_u_async/get_next_verify/verify_async_thread/verify_async_init/verify_async_exit/fio_verify_init/verify_header/populate_hdr/__fill_hdr/fill_hdr/14개 fill_<alg>/14개 verify_io_u_<alg>/verify_io_u_pattern/check_pattern/pattern_need_buffer/io_u_verify_off/log_verify_failure/hexdump/dump_buf/__dump_verify_buffers/dump_verify_buffers/verify_trimmed_io_u/mem_is_zero/mem_is_zero_slow/fill_buffer_pattern/__fill_buffer/fill_verify_pattern/fill_pattern_headers/get_hdr_inc/__hdr_size/hdr_size/hdr_priv/memswp/paste_blockoff/get_all_io_list/open_state_file/write_thread_list_state/__verify_save_state/verify_save_state/verify_free_state/verify_assign_state/verify_state_hdr/verify_load_state/verify_state_should_stop — 각각 @param/@return/동작 단계/실행 컨텍스트/호출 체인/에러 경로 포함); (e) struct vcont 7필드 §4 멀티라인(io_u/hdr_num/td/name/good_crc/bad_crc/crc_len — 입/출력 분리 + 설정자/독자/값범위/동기화 전수); (f) 모든 실행 라인 인라인 — FIO_HDR_MAGIC 0xacca·VERIFY_HEADER_VERSION 0x81 최상위 비트 풀이·검증 블록 바이트 슬롯·memswp 3단계 스왑 메커니즘·CRC32C offsetof(crc32)로 자기 제외·hweight8 XOR 비트 에러 진단·Rusty Russell "memcmp with self" 트릭 근거·asprintf aux_path elvis operator·fio_did_warn 재발 방지·atomic_load_acquire IP_F_IN_FLIGHT happens-before·pthread_cond broadcast/signal + detach 패턴·read_barrier + exit 플래그 빠른 탈출·flist_splice_init 락 최소화·O_SYNC 크래시 안전 근거·LE 직렬화 cpu_to_le64/le64_to_cpu·thread_io_list_sz 가변 크기·io_list_next 포인터 산술·RNG use64 32b/64b Tausworthe 분기·verify_state_should_stop 재시작 (a)/(b) 두 판정 경우·__fill_random_buf_percentage compress_chunk·paste_format "%o" specifier ↔ paste_blockoff 콜백 매핑·typecheck 타입 안전성·hdr_priv sizeof(verify_header) 오프셋·verify_offset 쓰기/검증 대칭 스왑·numberio 검증 3조건 AND(td_write & bs고정 & !time_based) + verify_write_sequence opt-in·IO_U_F_VER_IN_DEV HW PI(T10 DIF) 우회·FIO_FAKEIO null 엔진 우회·verify_fatal→fio_mark_td_terminate 잡 종료 예약·SSE4.2 PCLMUL / ARMv8 CRC32 HW probe·__rand 64비트 상위 엔트로피 보강 등 전수. 검증 경로(verify_io_u 14종 switch)와 쓰기 경로(populate_hdr 14종 switch)가 알고리즘 한 쌍으로 대칭. 비동기 오프로드(verify_io_u_async + verify_async_thread) pthread 풀 생성/종료 프로토콜 전면 명시. 재시작 지원(get_all_io_list/verify_save_state/verify_load_state/verify_state_should_stop) 상태 파일 레이아웃 + CRC32C 검증 + 두 판정 경우 전수. | 300 → 1348 (2281 → 3487 라인)
- 2026-04-18 | io_u.c | io_u 생명주기 허브(3874 라인) 함수 §2 대규모 재작성. 82개 함수 중 약 60개 함수 주석을 신기준(@param/@return/호출 체인/실행 컨텍스트/에러 경로/상태 전이)으로 재작성. 핵심 생명주기 6종(get_io_u/__get_io_u/put_io_u/io_completed/fill_io_u/set_io_u_file)은 ASCII 다이어그램 포함해서 상세 설명. 랜덤 분포 변종 7종(sprandom/uniform/zipf/pareto/gauss/zoned/zoned_abs)과 순차(get_next_seq_offset)에 수학적 배경·파라미터·호출 체인 추가. latency target 7함수(init/reset/check/failed/success/fatal/new_cycle)의 이진 탐색 알고리즘 서술. 통계 버킷 함수군(__io_u_mark_map/submit/complete/depth/lat_{n,u,m}sec/latency)에 버킷 경계·히스토그램 목적 명시. 파일 선택 4종(__get_next_fileno_rand/get_next_file_rand/rr/__get_next_file) + set_io_u_file 에 FSERVICE_* 분기표. 완료 회계(account_io_completion/init_icd/ios_completed/io_u_update_bytes_done/io_u_sync_complete/io_u_queued_complete/io_u_queued) 에 slat/clat/lat 계산식·stat.c 연계·verify 모드 분기. sync/trim 디스패처(do_io_u_sync/do_io_u_trim/do_sync_file_range) 에 fsync/fdatasync/syncfs/sync_file_range/os_trim/BLKDISCARD/PUNCH_HOLE syscall 매핑 + CONFIG_FCNTL_SYNC/FDATASYNC/HAVE_TRIM 빌드 분기 근거. 버퍼 관리(get_buf_state/save_buf_state/fill_io_buffer/io_u_fill_buffer/small_content_scramble) 에 dedupe/compress/pattern/zero/random 정책 + REPEAT/WORKING_SET 모드. 미완: 함수 본문 실행 라인별 §3 전수 인라인 보강은 mark_random_map/__get_next_rand_offset 만 시작 — 나머지 함수들의 기존 shallow 인라인은 유지(후속 세션 추가 패스 필요). 상단 블록·io_completion_data 구조체 §4·#include 블록은 이미 신기준이라 보존. | 1060 → 1596 (3874 → 4577 라인)
- 2026-04-18 | backend.c | fio 메인 실행 루프 핵심 모듈(4082 라인) 70-80% 신기준(◐ 유지, 1회 더 패스 필요). 상단 4섹션 재작성 + td->runstate 전이 다이어그램 삽입(TD_NOT_CREATED→CREATED→INITIALIZED→RAMP→RUNNING/VERIFYING/FSYNCING→FINISHING→EXITED→REAPED 전이 조건 포함); #include 11 시스템 + 19 내부 헤더 사유 전수 인라인; 전역 18개 + struct fork_data §4 멀티라인; 함수 §2 헤더 41/60+ 업그레이드(sig_int/sig_break/set_sig_handlers/check_min_rate/fio_io_sync/fio_file_fsync/fio_syncfs/update_ts_cache/runtime_exceeded/wait_for_completions/io_queue_event/in_flight_overlap/io_u_submit/do_verify/io_bytes/io_issue/io_complete_bytes_exceeded/usec_for_io/init_thinktime/handle_thinktime/log_inflight/invalidate_inflight/clear_inflight/do_io/init_inflight_logging/free_inflight_logging/cleanup_io_u/init_io_u/init_io_u_buffers/set_ioscheduler/switch_ioscheduler/keep_running/exec_string/do_dry_run/thread_main/reap_threads/trigger_*/check_mount_writes/waitee_running/run_threads/fio_backend). **미완료**: do_io/thread_main/run_threads 본문 수백 라인의 §3 전수 인라인 + __check_min_rate/handle_thinktime 본문. | 978 → 1743 (4082 → 5052 라인)
- 2026-04-18 | init.c | fio 잡 초기화·옵션 파싱 핵심 모듈(4364 라인 대형 파일) 신기준 마감. 상단 블록을 (1)CLI/INI/공유 SHM/fixup_options 네 대주제 분류 (2)_start→main→parse_options 호출 체인 ASCII (3)타 모듈 20+종 카탈로그(fio.h/parse.c/options.c/ioengines.c/smalloc.c/filesetup.c/filehash.c/verify.c/iolog.c/stat.c/steadystate.c/flow.c/filelock.c/profile.c/blktrace.c/server.c/client.c) (4)주요 함수·구조체 요약 (5)fio 복잡도 배경 설명으로 전면 재작성; #include 17종 사유 전수 인라인(stdio/stdlib/unistd/ctype/string/errno/sys/ipc/sys/types/dlfcn/valgrind/fio.h/sys/shm/parse.h/smalloc.h/filehash.h/verify.h/profile.h/server.h/idletime.h/filelock.h/steadystate.h/blktrace.h/oslib/crc/lib 각각 공급 심볼 카탈로그); 전역 37개 §4 멀티라인 재작성(설정자/읽는 자/값범위/동기화 4요소); l_opts[] 테이블 공통 규약 블록(struct option 필드 의미·FIO_CLIENT_FLAG 16번 비트 근거); 전 44개 함수 §2 전면 재작성 — 특히 parse_options/parse_cmd_line/__parse_jobs_ini/add_job/fixup_options/ioengine_load/setup_random_seeds/td_fill_rand_seeds/get_new_job/add_thread_segment/free_shm 핵심 함수에 처리 단계 번호 매김·실행 컨텍스트·에러 경로·호출 체인 ASCII 삽입; fixup_options 200+ 검증 지점을 [readonly]/[zone_mode]/[SPRandom]/[verify]/[compress]/[rate]/[dedupe]/[ss]/[FDP] 등 11 카테고리로 그룹화; enum FPRE_*/struct fpre_keyword §4 멀티라인; debug_levels[] 정적 테이블 §4. 기존 4000+ 라인 함수 내부 실행 라인 인라인은 대부분 충분히 깊이 있어 유지. | 1093 → 1652 (4364 → 5100 라인)
- 2026-04-18 | engines/net.c | TCP/UDP/Unix/VSOCK 네트워크 엔진 마감. fio_netio_queue/__fio_netio_queue/prep/open_file/setup/setup_splice/terminate/connect/accept/setup_listen_inet/send/recv/poll_wait/set_window_size/set_mss §2 전면 재작성. socket/bind/listen/accept/connect/sendto/recvfrom/send/recv/setsockopt 전수, SO_REUSEADDR/REUSEPORT/RCVBUF/SNDBUF/TCP_NODELAY/TCP_MAXSEG/IP_MULTICAST_IF/TTL/IP_ADD_MEMBERSHIP·MSG_WAITALL/MORE/NOSIGNAL/DONTWAIT/OOB/PEEK/TRUNC 카탈로그, pingpong RTT·UDP LINK_OPEN/CLOSE + udp_seq, splice/vmsplice 제로카피, FIO_SYNCIO\|DISKLESSIO\|UNIDIR\|PIPEIO\|BIT_BASED 플래그, CONFIG_LINUX_SPLICE/TCP_NODELAY/NET_WINDOWSIZE/NET_MSS/IPV6/VSOCK #ifdef 전수, listen 백로그(SYN/accept queue + somaxconn 클램프) | 542 → 567 (2493 → 2726 라인)
- 2026-04-18 | engines/io_uring.c | io_uring 엔진 두 변종(ioengine_uring + ioengine_uring_cmd passthru) 마감. ioengine_ops 22필드/25필드 각각 §4 멀티라인(.flags FIO_NO_OFFLOAD/ASYNCIO_SETS_ISSUE_TIME/ATOMICWRITES/MEMALIGN/RAWIO/MULTI_RANGE_TRIM/ASYNCIO_SYNC_SYNCFS + 미설정 비트 전수), constructor/destructor 본격 §2(.init_array/.fini_array·flist_add_tail/del_init), 10개 ZBD/FDP 위임 콜백 §2 전수 확장, fio_ioring_prep_md/cmd_fetch_ruhs §2, ARCH_HAVE_IOURING 빌드 가드 #endif 주석. 상단 4섹션·options[]·struct §4 는 이전 세션에 기완비 | 177 → 255 (3869 → 4152 라인)
- 2026-04-18 | engines/ime.c | DDN IME 세 엔진(ime_psync/ime_psyncv/ime_aio) 마감. CONFIG_IME 빌드 게이트 4단계 체인, IMESDK 매크로·im_client_cb_t 시그니처, 6 ddir 처리 매트릭스(VERIFY→READ 재디스패치, TRIM open_file 선거부, DATASYNC wrong ddir), fio_ime_psyncv_end 부분 전송 분배, fio_ime_aio_getevents max 초과 방지, 세 ioengine_ops 전 필드 §4, constructor/destructor §2 확장 | 372 → 419 (1841 → 1913 라인)
- 2026-04-18 | engines/mtd.c | /dev/mtdN MTD 엔진 마감. options[] 테이블 공통 규약 블록 + skip_bad 엔트리 전 필드 인라인(.name/.lname/.type/.off1/.help/.hide/.def/.category/.group) + NULL sentinel, ioengine_ops 9필드 §4(.flags FIO_SYNCIO\|FIO_NOEXTEND 풀이 + 미설정 13비트 전수), constructor/destructor 본격 §2(.init_array/.fini_array·libmtd_open/close·register/unregister_ioengine) | 61 → 84 (337 → 448 라인)
- 2026-04-18 | engines/cmdprio.h | libaio/io_uring/sg 공유 ioprio 믹스 헤더 마감. 상단 4섹션 재작성(Linux ioprio 비트 레이아웃 포함), enum 3값·struct cmdprio_prio 3·bsprio 4·bsprio_desc 2·options 5·cmdprio 4 필드 §4 멀티라인, CMDPRIO_OPTIONS 매크로 5엔트리 공통 규약 전 필드 인라인 + UNSUPPORTED 폴백 경로, 3 API §2 | 11 → 54 (250 → 373 라인)
- 2026-04-18 | engines/gfapi.h | GlusterFS gfapi 세 엔진(glusterfs/_sync/_async) 공유 헤더 마감. 상단 4섹션(TCP 24007/24008 MON 접속·setup→init→open_file→queue/event→cleanup 타임라인), struct gf_options 4필드·gf_data 3필드 §4 멀티라인, 6 extern 선언(gfapi_options + fio_gf_setup/cleanup/open_file/close_file/get_file_size/unlink_file) 각각 §2 | 9 → 19 (37 → 138 라인)

- 2026-04-18 | engines/nvme.c + engines/nvme.h | NVMe passthru 공용 헬퍼 라이브러리 + 헤더 동시 마감(io_uring_cmd/xnvme/sg 가 공유, ioengine_ops vtable 미등록 비-엔진). nvme.c: 기존 ◐(298/1386, 상단 4섹션·18함수 §2·실행 라인 인라인 대부분 신기준이었으나 비-엔진 성격 명시 부재)에서 본 세션에 (a) 상단 블록에 `=== 본 파일의 비-엔진 성격 ===`(공유 라이브러리 역할·register_ioengine 미호출·constructor/destructor 부재·정적 링크) + `=== ioctl(NVME_IOCTL_*) vs io_uring_cmd 경로 비교 ===`(Admin/IO 큐 동기 ioctl vs 비동기 IORING_OP_URING_CMD 매핑·NVME_URING_CMD_IO[_VEC] 매직 풀이) 두 보강 섹션 추가. nvme.h: 기존 ◐(39/621, 축약형 `=== 개요/주요 기능 영역/주요 구조체 ===` + 모든 필드 단일 라인 BAD 주석 + inline 함수 §2 부재)에서 본 세션에 (a) 상단 4섹션을 정확한 제목/순서로 전면 재작성(`=== 파일의 역할 ===`·`=== 전체 아키텍처에서의 위치 ===`(io_u→nvme_uring_cmd→IORING_OP_URING_CMD ASCII)·`=== 타 모듈과의 연결 ===`(linux/nvme_ioctl.h UAPI/fio.h/crc-t10dif.h/crc64.h/zbd_types.h/zbd.h)·`=== 주요 함수/구조체 요약 ===`) + 추가 3섹션(`=== NVMe 명령 CDW 레이아웃 핸드북 ===` Read/Write/Flush/DSM/Zone Mgmt/Identify CDW0~15 비트 슬롯 전수 + `=== Protection Information(PI) 동작 원리 ===` 16B T10 DIF/64B CRC64/PRACT/PRCHK_*/REFTAG/APPTAG/Escape 매직 + `=== ZNS / FDP / Streams 요약 ===`), (b) #include 2종 사유 인라인 확장(linux/nvme_ioctl.h UAPI 카탈로그/../fio.h fio 코어 심볼 카탈로그), (c) struct nvme_uring_cmd 18필드 §4 멀티라인 — 각 필드의 NVMe 스펙 비트 슬롯·설정자(SQE 빌더 함수)·읽는 자(커널 driver)·동기화·값 범위, (d) 매크로 11종 인라인 풀이(NVME_URING_CMD_IO[_VEC] _IOWR magic·NVME_DEFAULT_IOCTL_TIMEOUT 의 io_timeout 위임·NVME_IDENTIFY_DATA_SIZE/CSI_SHIFT·NVME_PI_APP/REF_DISABLE escape·NVME_ZNS_ZRA/ZRAS/ZSA·NVME_ZONE_TYPE_SEQWRITE_REQ·NVME_ATTRIBUTE_DEALLOCATE), (e) enum 7종 §4(nvme_identify_cns 4값/nvme_csi 3값/nvme_admin_opcode 1값/nvme_io_opcode 11값/nvme_zns_zs 7값/nvme_id_ctrl_ctratt ELBAS bit15/elbaf 비트 풀이/가드 타입), (f) struct §4 멀티라인 16종 재작성(nvme_data 9필드/nvme_pi_data 4필드/nvme_lbaf 3필드/nvme_16b_guard_pif 3필드/nvme_64b_guard_pif 3필드/nvme_id_ns 30+필드 fio 사용 강조 + 미사용 그룹/nvme_id_psd 14필드/nvme_id_ctrl 50+필드 + Fabrics Only 그룹/nvme_nvm_id_ns 4필드/nvme_zns_lbafe 3필드/nvme_zns_id_ns 14필드 mor 강조/nvme_zns_desc 9필드/nvme_zone_report 3필드/nvme_fdp_ruh_status_desc 5필드/nvme_fdp_ruh_status 3필드/nvme_dsm_range 3필드 nlb 1-based 주의/nvme_dsm 2필드/nvme_cmd_ext_io_opts 3필드), (g) inline 함수 6종 §2 + 모든 실행 라인 인라인(ilog2 floor(log2)+0→-1, put_unaligned_be48/get_unaligned_be48, fio_nvme_pi_ref_escape memcmp 매직, get_slba lba_ext 분기 시프트 가속, get_nlb 0-based -1 보정), (h) 외부 링크 함수 선언 10종에 caller/return 한 줄 §2. 두 파일 모두 비-엔진(헤더+공용 라이브러리)이므로 ioengine_ops·생성자/소멸자 항목은 본질적으로 부재(상단 블록에 명시). | nvme.c 298 → 298 (1386 → 1406 라인) / nvme.h 39 → 275 (621 → 1425 라인)
- 2026-04-18 | engines/rdma.c | RDMA(InfiniBand/RoCE/iWARP) 엔진(2193라인 대형 파일) 신기준 마감: 기존 ◐(280/2193, 상단 4섹션·struct §4·함수 §2·실행 라인 인라인이 어느 정도 갖춰져 있었으나 구기준 — 상단 블록의 ioengine_ops 콜백 매핑표/RKEY 교환 프로토콜/QP 상태 머신/Client·Server 모델/시스템 콜·라이브러리 카탈로그가 없었고, options[] 공통 규약 블록 부재, ioengine_ops 단일 상단 블록만 있고 필드별 §4 멀티라인 미흡, fio_rdmaio_init/post_init/connect/accept/setup_listen/setup_connect/close_file/send/cleanup/setup의 인라인 밀도 부족, constructor/destructor §2 짧음)에서 본 세션에 (a) 상단 4섹션을 대폭 확장 — 메모리 시맨틱(IBV_WR_RDMA_WRITE/READ) vs 채널 시맨틱(IBV_WR_SEND + 사전 post RECV) 두 모델, Client(initiator/active, hostname 옵션 + td_write) vs Server(target/passive, td_read) 자동 결정 룰, 양측 ioengine/port/verb/iodepth/bs 일치 필수 조건, ioengine_ops 콜백 11종 매핑표(setup→fio_rdmaio_setup … cleanup→fio_rdmaio_cleanup 전부), RKEY 교환 5단계(클라 SEND mode/iodepth → 서버 server_recv 검증·CHA_SEND→CHA_RECV 자동 전환 → 서버 SEND rmt_us[] → 클라 client_recv ntohl/__be64_to_cpu 복원 → 매 I/O마다 __rand%rmt_nr 인덱스 선택), 채널 시맨틱 RNR 회피용 클라 500ms usleep, 호출 체인 ASCII(backend.c → ioengines.c → fio_rdmaio_* → libibverbs/librdmacm → 커널 uverbs/rdma_cm → HCA 드라이버 mlx5/mlx4/qedr/cxgb4 → wire IB/RoCE/iWARP), 실행 컨텍스트(잡 스레드 단독·cmthread 필드 예약·rdma_get_cm_event 동기 호출), QP 상태 머신(RESET→INIT→RTR→RTS→ERR, rdma_cm이 ibv_modify_qp 자동 수행, ERR 상태에서 모든 post 실패하므로 wc.status 검사 중요), 의존 라이브러리 API 카탈로그(libibverbs 14종 + librdmacm 13종 + 표준 BSD 소켓 카탈로그), 데이터 흐름 풀 ASCII(io_u 할당 → orig_buffer 슬라이스 → ibv_reg_mr LOCAL_WRITE|REMOTE_READ|REMOTE_WRITE → io_u->mr->lkey → queue 적재 → commit fio_rdmaio_send/recv → ibv_post_send/recv → HCA DMA → wire → 원격 HCA → 원격 MR DMA → ACK → CQE 생성 → comp_channel fd 알림 → ibv_get_cq_event → ibv_poll_cq → cq_event_handler wr_id 매칭 → swap-remove → io_us_completed append → getevents 카운트 → event FIFO shift → put_io_u), 공유 상태(td->io_ops_data 잡 스레드 격리, fork 후 RDMA 자원 사용 금지로 use_thread=1 권장), 주요 함수/구조체 28개 요약, fio 사용법; (b) #include 17종 사유 전수 인라인 카탈로그화 — stdio/stdlib(usleep·calloc 용도)/unistd/errno/assert/netinet/in.h(sockaddr_in/in_addr/INADDR_ANY/htons/htonl/ntohl)/arpa/inet.h(inet_aton)/netdb.h(gethostbyname/hostent)/poll.h(미래 확장 대비)/sys/types.h/sys/socket.h(AF_INET/sockaddr_storage)/sys/time.h(timeval)/sys/resource.h(getrlimit/setrlimit/RLIMIT_MEMLOCK)/pthread.h(pthread_t 타입만)/inttypes.h(PRIx64/PRId64)/fio.h(FIO_Q_*/dprint/log_*/td_verror/fio_ro_check/fio_fill_issue_time/fio_gettime/td_set_runstate/add_file/init_rand_seed/io_u_queued/io_u_mark_submit/register_ioengine/unregister_ioengine/fio_init/fio_exit/FIO_DISKLESSIO/UNIDIR/PIPEIO/ASYNCIO_SETS_ISSUE_TIME/FIO_IOOPS_VERSION 전수)/hash.h(__rand/frand_state/GOLDEN_RATIO_64)/optgroup.h(FIO_OPT_G_RDMA)/rdma/rdma_cma.h(rdma_create_event_channel/create_id/resolve_addr/route/connect/listen/bind_addr/accept/disconnect/create_qp/destroy_id/get_cm_event/ack_cm_event 전수+rdma_cma.h가 verbs.h를 transitively include한다는 사실); (c) FIO_RDMA_MAX_IO_DEPTH=512 매크로 §4 두 가지 용도(rmt_us[] 정적 배열 크기 + 제어 메시지 wr_id 센티널), enum rdma_io_mode 5값(UNKNOWN/MEM_WRITE/MEM_READ/CHA_SEND/CHA_RECV) 각각 §4 멀티라인 — 설정자/독자/값범위/의미/동기화 전수, server_recv가 CHA_SEND→CHA_RECV 자동 전환 메커니즘 명시; (d) options[] 4엔트리에 공통 규약 블록 추가(.name/.lname/.type STR_STORE vs INT vs STR/.off1 offsetof/.cb 콜백/.def/.help/.category=FIO_OPT_C_ENGINE/.group=FIO_OPT_G_RDMA/.alias/.posval{ival,oval,help}/.minval-.maxval) + 엔트리별 .name/.lname/.type/.off1/.cb/.def/.help/.category/.group/.alias 전 필드 인라인(hostname str_hostname_cb 콜백 단독 처리 근거, bindname 빈 기본값 = 자동 인터페이스, port 1..65535 검증, verb posval 4종 매핑+alias=proto 레거시, NULL 센티널 parse.c 순회 종료); (e) ioengine_ops 16필드 §4 멀티라인 전면 재작성 — .name/.version FIO_IOOPS_VERSION ABI 가드/.setup·init·post_init 3단 초기화 순서/.prep·queue FIO_Q_QUEUED·BUSY 반환 계약/.commit is_client 분기/.getevents ibv_get_cq_event 블로킹/.event io_us_completed[0] FIFO/.cleanup setup calloc 짝/.open_file td_read 분기/.close_file MEM 모드 종료 통지/.flags 4비트 풀이(FIO_DISKLESSIO|UNIDIR|PIPEIO|ASYNCIO_SETS_ISSUE_TIME) + 미설정 비트(SYNCIO/RAWIO/NOEXTEND/BARRIER/NODISKUTIL) 의미 전수/.options/.option_struct_size td->eo 할당 크기; (f) constructor/destructor §2 확장(fio_init=__attribute__((constructor))→.init_array→ld.so가 main 전 자동 호출, register_ioengine=flist_add_tail&engine_list 링크, fio_exit=__attribute__((destructor))→.fini_array→ atexit 체인, unregister_ioengine=flist_del_init 재적재 안전성); (g) 함수 인라인 전수 보강 — fio_rdmaio_init(td_rw/td_random 거부 근거, rdma_create_event_channel fd 기반 통지, RDMA_PS_TCP=RC QP+TCP 유사 포트스페이스 vs RDMA_PS_UDP=UD QP, MEM 모드 rmt_us 사전 할당, io_us_*[3] 큐 3종, td_read=서버+TD_F_NO_PROGRESS), fio_rdmaio_post_init(io_u_freelist 순회, IBV_ACCESS LOCAL_WRITE+REMOTE_READ+REMOTE_WRITE 각 의미, lkey/rkey 발급, cpu_to_be64+htonl로 네트워크 직렬화, 클라도 같은 코드 실행하나 서버 응답이 덮어씀), fio_rdmaio_connect(conn_param 4필드 IB CM REQ 매핑·responder_resources/initiator_depth/retry_count IB 스펙 명시·retry_count=10 초과지만 librdmacm 7로 클램프, RDMA_CM_EVENT_ESTABLISHED vs REJECTED/UNREACHABLE, send_buf.mode/nr 통보, 500ms RNR 회피 채널 시맨틱 한정 의미), fio_rdmaio_accept(rdma_accept→IB CM REP→QP RTS 전이, 자식 cm_id 사용, send_buf post_init이 이미 rmt_us 채워둠), fio_rdmaio_close_file(MEM 모드 종료 통지 SEND 근거 — 서버 CPU 무개입이라 SEND뿐, sq_wr.wr_id=FIO_RDMA_MAX_IO_DEPTH 센티널, child_cm_id disconnect, 자원 역순 해제 순서 어긋남 명시 - 원본 그대로 QP→CQ→PD가 정석), fio_rdmaio_send(WRITE/READ rmt_us 인덱스 __rand 부하 분산, opcode·rkey·remote_addr·length 매 I/O 갱신, ibv_post_send=커널→HCA doorbell 비동기), fio_rdmaio_setup_connect(bindname dual-address resolve, RDMA_CM_EVENT_ADDR_RESOLVED 후 RDMA_CM_EVENT_ROUTE_RESOLVED 2단 해석, IB SA PathRecord vs RoCE/iWARP IP 라우팅 차이, 응답 수신용 RECV 사전 post 근거 - 안 하면 RNR), fio_rdmaio_setup_listen(TD_SETTING_UP 통계 왜곡 방지, INADDR_ANY vs htonl(*o->bindname) 레거시 버그성 동작 명시, backlog=3, RDMA_CM_EVENT_CONNECT_REQUEST 시 child_cm_id 저장, runstate 복원), aton(inet_aton/gethostbyname 폴백, h_addr=h_addr_list[0] 매크로, IPv4 전용·AAAA 미지원, td_verror 통계 보고), compat_options(레거시 "host/port/mode" 슬래시 구분자 역파싱, '/' 두 번 strchr+NUL 자르기, mode strncmp prefix 매칭 + 대소문자 양쪽 허용), fio_rdmaio_setup(td->files_index 더미 파일 추가, GOLDEN_RATIO_64=0x9E3779B97F4A7C15 시드 황금비 32비트), fio_rdmaio_cleanup(MR/QP/CQ/PD는 close_file에서 해제 가정, 내부 배열 free 누락 원본 한계 — 잡 종료 시 OS가 회수). 기존 client_recv/server_recv/cq_event_handler/rdma_poll_wait/fio_rdmaio_setup_qp/setup_control_msg_buffers/get_next_channel_event/prep/event/getevents/queue/queued/commit/recv/check_set_rlimits 함수와 struct rdmaio_options 4필드/struct remote_u 3필드/struct rdma_info_blk 4필드/struct rdma_io_u_data 4필드/struct rdmaio_data 21필드는 이미 §2 함수 주석 + §4 멀티라인 필드 주석 + 인라인이 신기준 수준이라 유지. | 280 → 402 (2193 → 2627 라인)
- 2026-04-18 | engines/xnvme.c | xNVMe 교차 백엔드 NVMe 엔진(2097라인 대형 파일) 신기준 마감: 기존 ◐(301/2097 라인, 상단 4섹션·struct §4·함수 §2·실행 라인 인라인 대부분 갖춰져 있었으나 구기준 수준 — 축약 4섹션·struct 필드 단일 라인 요약·options[] 공통 규약 미명시·ioengine_ops 단일 상단 블록)에서 본 세션에 (a) 상단 4섹션을 8종 백엔드 카탈로그(io_uring/io_uring_cmd/libaio/SPDK/thrpool/posix/vfio/nil)·io_uring_cmd=IORING_OP_URING_CMD+NVMe_URING_CMD_IO 매핑·nvme.c/sg.c/io_uring.c와의 기능 중첩과 고유 가치 "단일 코드 경로로 6~7종 백엔드 스위치"·ZBD/FDP 콜백이 init 이전 호출 가능해서 io_ops_data 있으면 재사용/없으면 임시 open/close 양쪽 지원·--thread=1 강제 근거(libxnvme 백엔드 레지스트리·PCI enumeration·SPDK EAL 전역 상태 fork 비호환)·백엔드별 시스템콜 분기(io_uring_cmd=io_uring_enter(2)/libaio=io_submit(2)+io_getevents(2)/SPDK=유저스페이스 폴링 0콜)·데이터 흐름 ASCII(io_u offset→>>ssw 또는 /lba_nbytes로 SLBA/NLB→cmd.nvm 필드 채움→PI generate(WRITE)→libxnvme→백엔드→커널/디바이스→완료 큐→xnvme_queue_poke→cb_pool→PI verify(READ)→iocq→getevents)·주요 함수 21개 요약 전개로 대폭 확장, (b) #include 7종(stdlib/assert/libxnvme/fio/verify/zbd_types/dataplacement/optgroup) 사유 전수 인라인 — libxnvme.h 가 백엔드별 내부 구현을 숨기고 불투명 포인터만 제공, abs()로 libxnvme 음수 에러코드→양수 errno 변환, verify.h VERIFY_NONE 이 확장LBA+PI+verify 삼자 배타 관계 근거, zbd_types.h 가 NVMe ZNS zs/zt 코드를 공용 ZBD_ZONE_COND_/ZBD_ZONE_TYPE_ 로 매핑해 zbd.c 가 백엔드 독립적으로 동작, dataplacement.h fio_ruhs_info nr_ruhs/plis[] 구조, optgroup.h FIO_OPT_G_XNVME 가 `fio --enghelp=xnvme` 필터링 태그, (c) g_serialize 전역 뮤텍스 §4 재작성, (d) struct xnvme_fioe_fwrap 7필드(fio_file/dev/geo/queue/ssw/lba_nbytes/md_nbytes/lba_pow2/_pad) + struct xnvme_fioe_data 9필드(iocq/completed/ecount/prev/cur/nopen/nallocated/iovec/md_iovec/files[]) + struct xnvme_fioe_request 2필드(pi_ctx/md_buf) + struct xnvme_fioe_options 16필드(hipri/sqpoll_thread/xnvme_dev_nsid/xnvme_iovec/md_per_io_size/pi_act PRINFO bit3/apptag/apptag_mask/prchk/xnvme_be/xnvme_mem/xnvme_async 8종/xnvme_sync/xnvme_admin/xnvme_dev_subnqn NQN) §4 멀티라인 전면 재작성(설정자·읽는 자·값 범위·동기화 전수), 64B 캐시라인 STATIC_ASSERT false sharing 완화 근거 명시, (e) options[] 15엔트리 공통 규약 블록(.name/.lname/.type FIO_OPT_STR_SET vs STR_STORE vs INT vs BOOL 구분/.off1 offsetof/.def 기본값/.help/.cb 사용자 파서/.category=FIO_OPT_C_ENGINE/.group=FIO_OPT_G_XNVME) + 전 필드 인라인 추가, (f) ioengine_ops 23필드 §4 멀티라인 전면 재작성(.name load_ioengine strcmp/.version FIO_IOOPS_VERSION ABI 가드/.options/.option_struct_size td->eo 할당/.flags 5비트 풀이 FIO_DISKLESSIO|NODISKUTIL|NOEXTEND|MEMALIGN|RAWIO + 미설정 비트(SYNCIO/PIPEIO/NOIO/ASYNCIO_SYNC_TRIM/FAKEIO) 의미/.cleanup close_ioengine/.init td_io_init/.iomem_free·iomem_alloc io_u.c 버퍼 경로/.io_u_free·io_u_init per-io_u/.event·getevents·queue FIO_Q_QUEUED·BUSY·COMPLETED 3-way 반환 계약/.close_file·open_file·get_file_size filesetup.c 위임/.invalidate 대부분 no-op/.get_max_open_zones·get_zoned_model·report_zones·reset_wp zbd.c 연계/.fdp_fetch_ruhs dataplacement.c 연계), (g) constructor/destructor §2 확장(fio_init __attribute__((constructor)) .init_array ld.so 로더가 main() 이전 자동 호출, register_ioengine=flist_add_tail 전역 engine_list 링크, fio_exit __attribute__((destructor)) .fini_array atexit 체인·dlclose 경로, unregister_ioengine=flist_del_init 재적재 안전성)으로 신기준 통과. 기존 cb_pool(pi_flags>>3 pi_act 역추출, pi_verify READ+pi_act=0 조건)·xnvme_fioe_queue(offset>>ssw vs offset/lba_nbytes 2의 거듭제곱 분기, pi_act=1+OOB=pi_size 컨트롤러 위임 보정, PI Type1/2 16B vs 64B GUARD ilbrt/cdw03 분할, Type3 REFTAG 없음 APPTAG만, passv vs pass 벡터드/단일 분기)·xnvme_fioe_getevents(라운드로빈 prev 커서 재개, -EBUSY/-EAGAIN usleep(1) 재시도)·_dev_open(pi_act 보정 후 lba_pow2 재설정, g_serialize 직렬화)·_verify_options(확장 LBA+PI+verify 배타)·report_zones(NVMe zs/zt → ZBD_ZONE_COND_EMPTY/IMP_OPEN/EXP_OPEN/CLOSED/FULL/OFFLINE + SWR 타입 매핑)·reset_wp(Zone Mgmt Send RESET opcode)·fetch_ruhs(IO Management Receive RUHS 서브오프코드)는 이미 상세 인라인 + §2 함수 주석으로 신기준 수준이라 유지. | 301 → 378 (2097 → 2372 라인)
- 2026-04-18 | engines/libaio.c | Linux 네이티브 AIO(libaio) 엔진 신기준 마감: 기존 ◐(227/1222, 상단 4섹션+struct §4+함수 §2는 이미 존재했으나 상단 블록 축약/options[] 및 ioengine_ops 필드별 §4 인라인 미달)에서 본 세션에 (a) 상단 4섹션을 5단계 엔진 매핑표(prep→queue→commit→getevents→event)·5개 syscall(io_setup/io_submit/io_getevents/io_cancel/io_destroy)·플래그 4비트(FIO_ASYNCIO_SYNC_TRIM/SYNC_SYNCFS/SETS_ISSUE_TIME/ATOMICWRITES)·RWF_* UAPI(NOWAIT/ATOMIC/HIPRI/UNCACHED/APPEND)·userspace_reap+aio_ring·cmdprio 공유 유틸·TD_F_CHILD RCU stall 우회·데이터 흐름 ASCII 도식·공유 상태 섹션으로 전면 확장, (b) #include 11종 사유 전수 인라인(libaio.h 공급 심볼 카탈로그 — io_context_t/struct iocb 필드/io_event/IO_CMD_* opcode/io_prep_pread·pwrite·preadv·pwritev·fsync·fdsync 매크로·io_queue_init/io_destroy/io_submit/io_getevents/io_cancel, errno의 EAGAIN/EINTR/ENOMEM/EINVAL, fio.h가 공급하는 DDIR_*/FIO_Q_*/ddir_rw/ddir_sync/fio_ro_check/do_io_u_trim/do_io_u_sync/io_u_mark_*/container_of/fio_gettime/generic_open/close/get_file_size/fio_init/fio_exit 전수, lib/pow2.h의 is_power_of_2, optgroup.h의 FIO_OPT_C_ENGINE/G_LIBAIO, lib/memalign.h 정렬 맥락, cmdprio.h의 struct cmdprio/enum mode/CMDPRIO_OPTIONS 매크로), IOCB_FLAG_IOPRIO=(1<<1) ABI 폴백 정의 사유(구형 aio_abi.h 미정의 시)와 iocb.aio_reqprio→BIO ioprio 전파 메커니즘, aio_rw_flags→__pad2 치환(libaio<0.3.111 호환)이 CONFIG_LIBAIO_RW_FLAGS configure 피처 검사로 활성화되는 경로, fio_libaio_commit/init 전방선언 사유(getevents 재시도 경로의 상호 호출), (c) options[] 5엔트리에 공통 규약 블록(.name/.lname/.type FIO_OPT_STR_SET vs BOOL 구분/.off1 offsetof/.help/.category=FIO_OPT_C_ENGINE/.group=FIO_OPT_G_LIBAIO)과 엔트리별 .name/.lname/.type/.off1/.help/.category/.group 전 필드 인라인 추가(userspace_reap의 논블로킹 제약·nowait RWF_NOWAIT 커널 지원 버전 4.14+/4.19+ 명시·libaio_vectored의 iov_count=1 고정 근거·CMDPRIO_OPTIONS 매크로가 전개하는 세부 옵션 열거·NULL sentinel parse.c 순회 종료), (d) ioengine_ops 14필드 §4 멀티라인 전면 재작성(.name 식별자·load_ioengine strcmp 연결/.version FIO_IOOPS_VERSION ABI 가드·check_engine_ops 거부 경로/.flags 4비트 각 풀이 + 미설정 비트(SYNCIO/DISKLESSIO/RAWIO/MEMALIGN/BARRIER/UNIDIR/NODISKUTIL) 의미 전수/.init post_init 분리 근거·자원 할당 순서/.post_init fork 이후 rlimit 조정 완료 후 호출되는 근거/.prep per-io_u 1회 호출·반환 규약 0/음수/.queue FIO_Q_QUEUED vs COMPLETED vs BUSY 3-way 반환·본 엔진 TRIM/SYNCFS 동기 폴백/.commit io_submit 루프·EAGAIN/ENOMEM/EINTR 재시도·부분 성공 허용/.getevents min/max/timespec 인자 의미·userspace_reap 분기 조건/.event container_of ev→iocb→io_u 역추적·error/resid 세팅/.cleanup TD_F_CHILD 분기 io_destroy 조건·RCU stall 우회/.open_file=generic_open_file filehash/open(2)/O_DIRECT 위임/.close_file=generic_close_file/.get_file_size=generic_get_file_size fstat/BLKGETSIZE64/.options 파싱 대상/.option_struct_size td->eo malloc 크기 결정), (e) constructor/destructor §2 확장(fio_init=__attribute__((constructor))→libc 동적 로더가 main 전 자동 호출, register_ioengine=flist_add_tail으로 전역 engine_list 등록, fio_exit=__attribute__((destructor))→atexit 체인에서 unregister_ioengine 호출)로 신기준 통과. 기존 구조체 3종(libaio_data 12필드/libaio_options 5필드/aio_ring 8필드) §4 멀티라인과 14개 함수 §2, 대부분의 실행 라인 인라인은 이미 신기준 수준이라 유지. | 227 → 252 (1222 → 1427 라인)
- 2026-04-18 | engines/solarisaio.c | Solaris 네이티브 AIO 엔진(aioread/aiowrite/aiowait/aio_result_t) 신기준 마감: 상단 4섹션 대폭 확장(Solaris 고유 AIO vs POSIX AIO(aio.h) 차이 — aio_result_t 는 사용자 구조체에 커널이 완료 시 aio_return/aio_errno 직접 기록, aiowait 는 "프로세스 전역 아무거나 완료 1건" 반환해 POSIX AIO 의 aio_suspend(aiocb 리스트 지정) 와 달리 별도 in-flight 리스트 관리 불필요, DDIR_SYNC/DATASYNC 는 Solaris AIO 에 비동기 fsync opcode 가 없어 드레인(sd->nr==0)+fsync(2)/fdatasync(2) 동기 폴백, SIGIO 시그널 기반 비동기 완료 수확 모드 USE_SIGNAL_COMPLETIONS 빌드 옵션 — 핸들러는 비동기 시그널 컨텍스트라 aiowait 의 async-signal-safety 우려, 생산자(SIGIO 핸들러)-소비자(getevents) 순서를 write_barrier 로 보장, MAXASYNCHIO(보통 256) 상한으로 iodepth 클램프 후 log_info), #include 7종 사유 전수 인라인(sys/asynch.h 가 공급하는 aio_result_t{aio_return, aio_errno}·aioread(fd,buf,sz,off,whence,resp)·aiowrite·aiowait(tv)·AIO_INPROGRESS·MAXASYNCHIO 카탈로그화, signal.h 의 sigaction/SA_RESTART/struct sigaction 용도, errno.h 의 EINVAL=대기할 요청 없음 정상 조기반환 근거, fio.h 가 공급하는 container_of/write_barrier/fio_init/fio_exit/register_ioengine/generic_open_file/generic_close_file/generic_get_file_size 열거), struct solarisaio_data 4필드(aio_events/aio_pending/nr/max_depth) §4 멀티라인 재작성(설정자/독자/값범위/동기화 — SIGIO 경로에서 단일 생산자·단일 소비자 write_barrier 근거, unsigned int 단일 워드 원자성 가정), 전 11함수 §2(prep/wait_for_event/getevents/event/queue/cleanup/sigio/init_sigio/init/register/unregister) — 각각 @param/@return/동작 단계/실행 컨텍스트/호출 체인/에러 처리 블록 포함, 모든 실행 라인 인라인(container_of(res, struct io_u, resultp) offsetof 역산 이디엄, aiowait 반환 규약 3분기((aio_result_t*)-1 실패/NULL 타임아웃/유효 포인터), EINVAL 무감 조기반환 vs 기타 errno → log_err+exit 치명 처리, timespec.tv_nsec/1000→timeval.tv_usec 다운컨버전, tv={0,0} 폴링 vs tv=NULL 무한 vs 지정 타임아웃, MAXASYNCHIO 선제 클램프 이유(초과 제출 시 EAGAIN/ENOMEM 실패), DDIR_SYNC/DATASYNC 의 sd->nr!=0 드레인 요구, FIO_Q_BUSY→코어 재시도/FIO_Q_COMPLETED 즉시완료/FIO_Q_QUEUED 비동기 큐잉 계약, aioread/aiowrite 6인자 시맨틱(whence=SEEK_SET 절대 오프셋), aio_return>=0 성공/부분전송·aio_return<0 실패→aio_errno 이관, sd->aio_events[sd->aio_pending++] 후 write_barrier 순서, sd->nr-- in-flight 회수, td_verror("xfer") 라벨, fio_ro_check readonly 가드, SA_RESTART EINTR 최소화 효과), USE_SIGNAL_COMPLETIONS #ifdef 분기 조건·장단점(잡스레드 잠잠 시 즉시 수확 vs 시그널 컨텍스트 제약) 명시, ioengine_ops 11필드 §4 멀티라인(.name/.version ABI/.init/.prep/.queue/.getevents/.event/.cleanup/.open_file=generic_open_file/.close_file/.get_file_size 각각 설정자·읽는 자·호출 시점·동기화, .flags=0 의미 — FIO_SYNCIO/DISKLESSIO/RAWIO/NOEXTEND/MEMALIGN/BARRIER/UNIDIR/NODISKUTIL/ASYNCIO_SYNC_TRIM/ASYNCIO_SYNC_SYNCFS 모두 미설정 이유 전수), constructor/destructor §2(fio_init/fio_exit GCC __attribute__((constructor))/((destructor)) 메커니즘, engine_list flist_add_tail/flist_del_init 등록/해제) | 118 → 585 (388 → 916 라인)
- 2026-04-18 | engines/rbd.c | Ceph RBD 비동기 엔진 신기준 마감: 파일 자체는 이미 상단 4섹션/struct §4/함수 §2/실행 라인 인라인이 갖춰진 고완성도 상태였고, 본 세션에서 미완이던 (a) options[] 7엔트리 필드별 인라인(공통 규약 블록 포함 — .name/.lname/.type/.off1/.help/.def/.category/.group 각 필드 의미), (b) ioengine_ops 14필드 §4 멀티라인(.name/.version/.setup/.init/.queue FIO_Q_QUEUED·COMPLETED 계약/.getevents/.event/.cleanup/.open_file/.invalidate/.options/.io_u_init/.io_u_free/.option_struct_size), (c) constructor/destructor §2 확장(fio_init=.init_array ld.so 초기화 맥락·register_ioengine=flist_add_tail·fio_exit=.fini_array atexit 체인)을 보강. rados_create2 vs rados_create 분기 근거(clientname 'client.' 자동접두), rados_conf_read_file/rados_connect CephX 인증·MON TCP, rados_ioctx_create pool 바인딩, rbd_open snap=NULL head image, rbd_set_image_notification+eventfd(EFD_SEMAPHORE)+poll(2)+rbd_poll_io_events 완료 수신 경로, busy_poll wait 전환, sort_events qsort age-based fairness, rbd_aio_{read,write,discard,flush,create_completion,get_return_value,release,wait_for_complete} API 카탈로그, rbd_encryption_load LUKS1/2, CONFIG_RBD_POLL/ENCRYPTION/INVAL 3 #ifdef 분기 의미 모두 주석 내에서 참조 가능. | 156 → 193 (1260 → 1375 라인)
- 2026-04-18 | engines/sync.c | 5개 동기 엔진(sync/psync/vsync/pvsync/pvsync2) 단일 파일 신기준 마감: 상단 4섹션 확장(한 파일 5엔진 공존 근거 — 공유 syncio_data/fio_io_end/LAST_POS/do_io_u_trim/sync 인프라, 각 엔진별 콜백 채움 표와 호출 체인, 실행 컨텍스트 — 잡 스레드 동기 블록), #include 6종 사유 전수(stdio/stdlib/unistd/sys/uio.h의 iovec·readv/writev/preadv/pwritev/preadv2/pwritev2 공급, errno의 EAGAIN, fio.h의 FIO_Q_*/do_io_u_trim/sync·ddir_rw/sync·fio_ro_check·io_u_mark_submit·generic_open/close/get_file_size 카탈로그, optgroup.h의 FIO_OPT_C_ENGINE/G_INVALID, lib/rand.h의 frand_state/init_rand/rand_between), LAST_POS(f) 매크로 §4(설정자는 fio_io_end의 성공 R/W 후 offset+ret, 독자는 fio_syncio_prep 단독 — sync만 이 최적화 사용, 값범위 0..파일크기 또는 -1ULL, 왜 필요한가 — read/write 파일 포인터 자동 전진 → 순차 I/O에서 lseek 생략해 syscall 절반 감소), syncio_data 9필드 §4 멀티라인(iovecs/io_us/queued/events/queued_bytes/last_offset/last_file/last_ddir/rand_state 각 엔진별 사용량 명시 — vsync 전체 활용 vs pvsync iovecs[0]만 vs pvsync2 iovecs[0]+rand_state), psyncv2_options 5필드 §4(pad/hipri/hipri_percentage/uncached/nowait — RWF_HIPRI block-layer polling/RWF_DONTCACHE=RWF_UNCACHED 버퍼드 캐시 우회/RWF_NOWAIT EAGAIN/RWF_ATOMIC torn-write 방지 플래그 의미·적용 조건 풀이), options[] 테이블 5엔트리 전 필드 인라인(.name/.lname/.type FIO_OPT_STR_SET vs INT vs BOOL 구분/.off1 offsetof/.minval/.maxval/.def 기본값/.help/.category=FIO_OPT_C_ENGINE/.group=FIO_OPT_G_INVALID + NULL 센티널 설명), 전 함수 §2 14개(fio_syncio_prep LAST_POS 최적화 상세/fio_io_end 반환값 → resid·error 환산 3단계/fio_pvsyncio_queue preadv·pwritev 즉시 실행/fio_pvsyncio2_queue RWF_* 플래그 조합/fio_psyncio_queue pread·pwrite/fio_syncio_queue read·write/fio_vsyncio_getevents 카운터 소비/fio_vsyncio_event io_us[idx] 반환/fio_vsyncio_append 3조건 AND 병합 판정/fio_vsyncio_set_iov iovec+last_* 캐시 갱신/fio_vsyncio_queue 4-way 분기 FIO_Q_QUEUED·BUSY·COMPLETED/fio_vsyncio_end 부분 전송 바이트 분배 + errno 선캡처/fio_vsyncio_commit lseek+readv·writev 일괄/fio_vsyncio_init·cleanup 자원 수명), 모든 실행 라인 인라인(ddir_rw/ddir_sync 매크로 의미, fio_ro_check assert, do_io_u_trim BLKDISCARD/PUNCH_HOLE 경로, do_io_u_sync fsync/fdatasync/sync_file_range 분기, RWF_ATOMIC은 write 한정 FIO_ATOMICWRITES 연동, hipri_percentage 확률 추첨 rand_between [1,100] <= percentage, O_DIRECT+RWF_DONTCACHE 중복 skip 근거, vsync의 FIO_Q_BUSY → 코어 commit → 재시도 라이프사이클, readv/writev 파일 포인터 의존이라 commit에서 lseek 선행, 부분 전송 시 iovec 순서대로 this_io cap 후 resid 분배, errno 루프 전 선캡처 이유), 5개 ioengine_ops 각각 §4 멀티라인(ioengine_rw/_prw/_vrw/_pvrw/_pvrw2 — 각 .name/.version ABI/.prep·.init·.cleanup·.queue·.commit·.event·.getevents·.open_file·.close_file·.get_file_size·.flags·.options·.option_struct_size 필드별 설정자/읽는 자/값범위/동기화 + FIO_SYNCIO(0x01)·FIO_SYNCFS(0x8000)·FIO_ATOMICWRITES(0x40000) 비트 풀이 + sync만 prep 사용·psync prep 생략·vsync commit/event/getevents 필요·pvsync2 options·option_struct_size 추가 등 콜백 차이 명시), constructor/destructor §2(5개를 한 constructor에서 등록하는 이유 — 공유 인프라 단일 translation unit 관리, fio_init __attribute__((constructor)) libc 동적 로더 자동 호출 메커니즘, register_ioengine=flist_add_tail 전역 engine_list 링크, CONFIG_PWRITEV/FIO_HAVE_PWRITEV2 조건부 컴파일 분기) | 93 → 264 (809 → 1501 라인)
- 2026-04-18 | engines/windowsaio.c | Windows IOCP 비동기 엔진 신기준 재작성: 상단 4섹션(IOCP 원리·FILE_FLAG_OVERLAPPED와 io_u 1:1 OVERLAPPED lifetime rule·두 완료 모드 분기·CONTAINING_RECORD 이디엄), #include 7종 사유 전수 인라인(stdio/stdlib/unistd/signal/errno/fio.h/optgroup.h + fio.h가 공급하는 FIO_Q_* 매크로·fio_file::hFile·io_u_qiter 등 카탈로그), CANCELIOEX 함수포인터 typedef 사용처 부재 설명(I/O 취소 미구현 전방선언), geterrno_from_win_error 전방 선언 근거, fio_overlapped 3필드(OVERLAPPED의 Internal/InternalHigh/Offset/OffsetHigh/hEvent 세부 의미 + NTSTATUS → POSIX errno 변환)·windowsaio_data 5필드(aio_events/iocp/iothread/iocomplete_event/iothread_running)·thread_ctx 2필드·windowsaio_options 2필드 §4 멀티라인(설정자/독자/값범위/동기화), options[] 단일 엔트리(no_completion_thread) .name/.lname/.type/.off1/.help/.category/.group 전 필드 인라인, 전 함수 §2(fio_windowsaio_init/cleanup/windowsaio_invalidate_cache/open_file/close_file/timeout_expired/event/getevents_nothread/getevents_thread/getevents/queue/IoCompletionRoutine/io_u_init/io_u_free + constructor/destructor), CreateEvent 4인자 의미(auto-reset FALSE, 초기 non-signaled) + CreateIoCompletionPort 2회 용도(빈 포트 생성 vs 파일 바인딩) + CreateFile 7인자 + GetQueuedCompletionStatusEx 6인자 + WriteFile/ReadFile lpNumberOfBytes=NULL 비동기 계약 라인 인라인, FILE_FLAG_POSIX_SEMANTICS|OVERLAPPED 기본+NO_BUFFERING(odirect)|WRITE_THROUGH(sync_io) 조합, fadvise_hint → FILE_FLAG_RANDOM_ACCESS/SEQUENTIAL_SCAN 5-way switch, invalidate_cache가 Cache Manager 드롭 유도하는 NO_BUFFERING 열고-닫기 트릭의 제약(시스템 유일 열린 핸들 필요), DDIR_TRIM 미지원 근거(Win32 블록 레벨 DISCARD 개별 I/O 미노출), timeout_expired의 GetTickCount 49.7일 wrap-safe 분기 2종, queue의 OVERLAPPED.Offset/OffsetHigh 64→32+32 분할, success||ERROR_IO_PENDING → FIO_Q_QUEUED 통합 처리(동기 완료도 IOCP에 패킷 큐잉되므로 수거 경로 일원화), FlushFileBuffers 동기 호출(OVERLAPPED 버전 부재), IoCompletionRoutine의 250ms 타임아웃 + iothread_running 체크 주기·CloseHandle(ctx->iocp) 소유권 이전·free(ctx) 정리, fov->io_complete 단방향 플래그의 SetEvent/Wait 메모리 배리어, ioengine_ops 13필드 §4(.flags=0의 FIO_SYNCIO/DISKLESSIO/NOEXTEND/RAWIO/MEMALIGN/BARRIER/UNIDIR/NODISKUTIL/FAKEIO 모두 미세트 이유 전수), fio_init/fio_exit constructor/destructor 계약 설명 | 71 → 266 (836 → 1417 라인)
- 2026-04-18 | engines/libpmem.c | PMDK libpmem 엔진의 ioengine_ops 전 필드 §4 확장으로 신기준 마감: 기존 상단 4섹션/함수 §2/인라인은 이미 신기준 수준이었고, 최종 미완이었던 ioengine_ops vtable을 필드별 §4 멀티라인(설정자/읽는 자/호출 시점/계약)으로 재작성. flags 비트마스크 FIO_SYNCIO|RAWIO|DISKLESSIO|NOEXTEND|NODISKUTIL|BARRIER|MEMALIGN 각 비트의 의미(SYNCIO→queue=완료·commit/getevents 건너뜀, DISKLESSIO→페이지캐시/블록레이어 우회, NOEXTEND→잡 중 파일 확장 금지 SIGBUS 방어, BARRIER→pmem_drain 메모리 배리어 추적, MEMALIGN→NT-store 정렬 요구) 풀이. 상단 구조체 서두의 한국어 요약 주석은 "구조체 수명·공유·동기화" 블록으로 재작성 | 83 → 신기준
- 2026-04-18 | engines/e4defrag.c | ext4 온라인 조각 모음 엔진 신기준 마감: options[] 테이블 2엔트리(.name/.lname/.type/.off1/.help/.category/.group + inplace의 .minval/.maxval) 전 필드 인라인 추가, NULL 센티널 주석, ioengine_ops 전 12필드 §4 멀티라인(.name/.version ABI 가드/.init/.queue/.open_file·close_file·get_file_size의 generic_* 재사용 근거/.flags=FIO_SYNCIO 단일 비트 풀이/.cleanup/.options/.option_struct_size), constructor/destructor §2(fio_init/.init_array와 fio_exit/.fini_array 메커니즘, register_ioengine=flist_add_tail 풀이). 기존 상단 4섹션/struct §4/함수 §2/실행 라인 인라인은 이미 신기준 | 83 → 신기준
- 2026-04-17 | engines/posixaio.c | POSIX AIO(librt) 엔진 신기준 마감: 상단 4섹션(ASYNCIO_SYNC_TRIM|SYNCFS 플래그로 TRIM/syncfs 동기 폴백, SIGEV_NONE 폴링 모델, aio_suspend 8개 배치 대기, OSX EAGAIN→FIO_Q_BUSY 백프레셔 이유), #include 6종 사유(errno의 EINPROGRESS/ECANCELED/EAGAIN, O_SYNC→aio_fsync 메타+데이터 동기화), posixaio_data 2필드 §4(aio_events/queued), fio_posixaio_prep/queue/getevents/event/init/cleanup 전 함수 §2, CONFIG_POSIXAIO_FSYNC #ifdef 분기 근거(aio_fsync 미지원 플랫폼 do_io_u_sync 드레인 폴백), io_u_qiter+seen 플래그+IO_U_F_FLIGHT 가드, aio_return 결과→io_u->resid 환산, ioengine_ops 전 필드 §4(FIO_ASYNCIO_SYNC_TRIM|SYNCFS 비트 풀이, generic_open/close/get_file_size 위임 근거), constructor/destructor §2 | 83 → 신기준
- 2026-04-17 | engines/nbd.c | libnbd 기반 NBD 엔진 신기준 마감: 상단 4섹션에 맞춰 #include 8종 사유 인라인(libnbd API 카탈로그: nbd_create/connect_uri/aio_pread/pwrite/trim/flush/nbd_poll/aio_peek_command_completed/aio_command_completed/get_error/get_errno), nbd_data 4필드(nbd/debug/completed/nr_completed)·nbd_options 2필드(padding/uri) §4, 옵션 테이블 .name/.lname/.help/.category/.group/.type/.off1 전 필드 인라인, nbd_setup(add_file "nbd" 가짜 파일 + 동기 프로브 + export size 취득 후 close)/nbd_init(잡별 본 AIO 연결)/cmd_completed(user_data→io_u + realloc append + error 전파)/nbd_queue(4 ddir 분기 aio_* + NBD_MAX_REQUEST_SIZE=64MiB assert + nbd_get_errno 폴백 EIO)/retire_commands(peek+command_completed 쌍으로 cookie 회수)/nbd_getevents(nbd_poll timeout ms 변환 + while 루프)/nbd_event(LIFO pop)/nbd_io_u_init/free/open_file/invalidate 전 함수 §2, ioengine_ops 전 필드 §4(FIO_DISKLESSIO|NOEXTEND 비트 풀이, 비동기 계약이라 FIO_SYNCIO 부재), constructor/destructor §2 | 66 → 신기준
- 2026-04-17 | engines/mmap.c | mmap(2) 동기 엔진 신기준 마감: #include 7종 사유(sys/mman.h가 mmap/munmap/msync/madvise/posix_madvise + PROT_/MAP_/MS_/POSIX_MADV_ 매크로 공급, verify.h의 VERIFY_NONE 참조), mmap_options 2필드(pad/thp) §4 멀티라인(MAP_PRIVATE+MADV_HUGEPAGE 분기 근거), 옵션 테이블 전 필드 인라인(FIO_OPT_INT 역사적 이유, FIO_OPT_G_MMAP), MMAP_TOTAL_SZ=1GiB 32비트 가상주소 고갈 방지 주석, mmap_map_size 전역 §4(잡 시작 후 불변, nr_files 분할), fio_mmap_data 3필드 §4(mmap_ptr/mmap_sz/mmap_off 설정자·독자·값범위·동기화), ioengine_ops 전 필드 §4(FIO_SYNCIO|FIO_NOEXTEND 비트 풀이: SYNCIO → queue 즉시 완료+submit/complete 자동 처리, NOEXTEND → 사전 매핑 범위 외 확장 금지 SIGBUS 회피), CONFIG_HAVE_THP 분기 근거, constructor/destructor §2 | 65 → 신기준
- 2026-04-17 | engines/skeleton_external.c | 외부(.so) 엔진 템플릿 상태 확정(●): 이미 상단 블록 4섹션+#include 7종 사유+fio_skeleton_options 2필드(pad/dummy) §4+FIO_OPT_STR_SET 옵션 풀이+전 9함수 §2(event/getevents/queue/prep/init/cleanup/open/close + ZBD 4종)+ZBD 모델(ZBD_NONE/HOST_AWARE/HOST_MANAGED NVMe ZNS 맥락) 설명+ioengine_ops 전 15필드 §4(fio_skeleton_* 콜백별 계약 + option_struct_size 역할)까지 갖춰져 있음. 다른 엔진 작업 시 null.c와 함께 참조용. | 62 → 신기준(기존 작업 확정)
- 2026-04-15 | engines/libblkio.c | libblkio 통합 블록 I/O 엔진 신기준 재작성: 상단 4섹션(io_uring/nvme-io_uring/virtio-blk-vhost-user·vhost-vdpa/vfio-user 드라이버 추상화, proc_state 프로세스 공유 blkio + per-thread blkioq, 참조 카운팅 기반 생성/파괴, 3종 wait_mode 전략 — BLOCK: blkioq_do_io 블로킹 / EVENTFD: non-block 수확 + completion eventfd blocking read / LOOP: busy-poll, hipri+EVENTFD 상호배타성, threaded subjob 간 driver/path/num_entries 일관성 검증 경로, setup→init→post_init→queue→getevents→event→cleanup 체인, FIO_DISKLESSIO|NOEXTEND|NO_OFFLOAD|SKIPPABLE_IOMEM_ALLOC 플래그 풀이), #include 8종 사유(assert/errno/stdbool/stddef/stdint/stdlib/string/blkio 각 라이브러리 API 연결 인라인), proc_state 4필드(mutex/initted_threads/initted_hipri_threads/b) §4 멀티라인 설정자·독자·값범위·동기화, fio_blkio_data 6필드(q/completion_fd/has_mem_region/mem_region/iovecs/completions) §4, fio_blkio_options 10필드(driver/path/pre_connect_props/num_entries/queue_size/pre_start_props/hipri/vectored/write_zeroes_on_trim/wait_mode/force_enable_completion_eventfd) §4, enum fio_blkio_wait_mode 3값 멀티라인, first_threaded_subjob 3필드·incompatible_threaded_subjob_options 전역 인라인, 전 함수 §2(proc_lock/unlock/set_props_from_str/log_err 매크로/possibly_null_strs_equal/total_threaded_subjobs/log_opt_compat_err/check_opt_compat/create_and_connect/setup/init/post_init/cleanup/iomem_alloc/iomem_free/open_file/queue/getevents/event/register/unregister), blkio 생명주기 create→set_*→connect→set num-entries/queue-size→set pre_start_props→set num-(poll-)queues→start 경로와 ENOENT(direct 속성 없는 드라이버) 허용 처리, blkio_get_queue vs blkio_get_poll_queue 인덱싱(initted_threads - initted_hipri_threads vs initted_hipri_threads) 근거, mem-region-alignment 조회+align_up+blkio_alloc_mem_region+blkio_map_mem_region 순서, post_init에서 td->orig_buffer 사후 mem_region 등록(iomem_alloc 미호출 시), blkioq_{read,write,readv,writev,discard,write_zeroes,flush} ddir 매핑 전 분기 라인 인라인, blkioq_do_io min/max 의미와 3 wait_mode 분기 루프 구조, blkio_completion.user_data→io_u 복원 + ret<0→+errno 변환, fio_ro_check/disable_slat=1/O_NONBLOCK 해제 이유, ioengine_ops 전 필드 §4(FIO_DISKLESSIO|NOEXTEND|NO_OFFLOAD|SKIPPABLE_IOMEM_ALLOC 각 비트 설명), fio_init/fio_exit constructor/destructor 계약 | 20 → 273 (라인 950 → 1459)
- 2026-04-15 | engines/libcufile.c | NVIDIA GPUDirect Storage(GDS) 엔진 신기준 재작성: 상단 4섹션(GDS P2P DMA 원리 — nvidia-fs 커널 모듈이 NVMe PRP 엔트리를 호스트 물리주소 대신 GPU BAR1 매핑 물리주소로 채워 스토리지↔GPU 직접 DMA로 호스트 페이지캐시/CPU 우회, cuFileBufRegister가 GPU 버퍼 pin하지 않으면 bounce 버퍼 사용하여 GDS 이점 상실, cuda_io=cufile vs posix(pread+cudaMemcpy) baseline 비교 목적, 호출 체인 backend→init(첫 워커 cuFileDriverOpen refcount+cudaSetDevice 바인딩)→iomem_alloc(cudaMalloc+cudaMemset 0xab+cuFileBufRegister pin)→open_file(cuFileHandleRegister fd→CUfileHandle OPAQUE_FD)→queue(동기 cuFileRead/Write P2P DMA)→close/iomem_free/cleanup 마지막 워커 cuFileDriverClose), 모든 #include 12종 사유 인라인(cufile.h/cuda.h/cuda_runtime.h/pthread.h/fio.h/optgroup.h/memalign.h/pow2.h), ALIGNED_4KB 매크로·LOGGED_* 비트·GPU_ID_SEP·IO_CUFILE/IO_POSIX enum 풀이, 전역 running/cufile_initialized/running_lock refcount 패턴, check_cudaruntimecall do-while(0) 이디엄, libcufile_options 8필드 §4(td/gpu_ids/cu_mem_ptr/junk_buf/my_gpu_id/cuda_io/total_mem/logged 각 설정자·독자·값범위·동기화), fio_libcufile_data 2필드 §4(cf_descr.handle.fd+OPAQUE_FD, cf_handle 불투명), fio_option 테이블 gpu_dev_ids/cuda_io posval 인라인, 전 함수 §2(get_cuda_error/find_gpu_id/init/pre_write/post_read/queue/open_file/close_file/iomem_alloc/iomem_free/cleanup/register/unregister), find_gpu_id의 strdup+strsep+subjob_number%id_count 라운드로빈, pre_write/post_read의 IO_CUFILE+verify(H2D seed)/IO_POSIX(D2H·H2D cudaMemcpy 오버헤드 재현) 분기 근거, queue의 4KB 정렬 경고 1회·while(remaining) 부분전송 루프·cuFileRead/Write(devPtr_base, size, file_offset, devPtr_offset) 4인자 의미·FIO_Q_COMPLETED 동기 계약, iomem_alloc의 td->orig_buffer 더미(fio 코어 io_u->buf 추적)+junk_buf+cudaMalloc+cudaMemset(0xab read-before-write 검증)+cuFileBufRegister pin 순서 및 실패 시 역순 해제, ioengine_ops 전 필드 §4(FIO_SYNCIO), fio_init/fio_exit constructor/destructor | 12 → 신기준
- 2026-04-15 | engines/libhdfs.c | Hadoop HDFS libhdfs JNI 엔진 신기준 재작성: 상단 4섹션(HDFS write-once/append-only 제약과 chunk_size 분할 전략, 전역 offset→(f_id=floor/chunk, 내부 offset=offset%chunk) 매핑, setup→init(hdfsBuilder→hdfsBuilderConnect)→io_u_init(프리할당 zero-fill)→prep(청크 전환·ddir별 open_flags)→queue(hdfsSeek/Read|readDirect|Write|Flush)→close→io_u_free(hdfsDisconnect) 체인, thread-local JVM/프로세스당 JVM 하나 제약, single_instance=1 FileSystem 캐시 공유 vs ForceNewInstance 격리, FIO_SYNCIO+FIO_DISKLESSIO+FIO_NODISKUTIL 플래그 풀이), #include 4종(math.h/hdfs.h/fio.h/optgroup.h)·매크로 2개(CHUNCK_NAME_LENGTH_MAX/CHUNCK_CREATION_BUFFER_SIZE) 인라인, hdfsio_data 3필드(fs/fp/curr_file_id sentinel -1)·hdfsio_options 7필드(pad/host/directory/port/chunck_size/single_instance/use_direct) §4 멀티라인(설정자/독자/값범위/동기화), 옵션 테이블 7엔트리 전 필드 인라인(namenode·hostname alias, port 1~65535, hdfsdirectory, chunk_size alias chunck_size, single_instance 기본 ON, hdfs_use_direct readDirect), 전 함수 §2(get_chunck_name/prep/queue/open_file/close_file/io_u_init/setup/init/io_u_free/register/unregister), 모든 실행 라인 인라인(O_WRONLY truncate 주의, hdfsSeek 실패=HDFS write seek 제한 경고, hdfsRead vs readDirect JNI 복사 차이, hdfsFlush가 hflush/hsync 아니어서 내구성 제한, 64KiB 버퍼로 hdfsWrite 반복, 포트 0/host NULL EINVAL, hdfsExists+hdfsSetWorkingDirectory 이중 검증 이유), ioengine_ops 전 필드 §4 | 12 → 신기준
- 2026-04-15 | engines/libiscsi.c | 유저스페이스 iSCSI initiator 엔진 신기준 재작성: 상단 4섹션(libiscsi가 커널 open-iscsi/tcm_loop 우회하고 TCP 위에 iSCSI PDU 조립, URL=iscsi://[user%pw@]host[:port]/iqn/lun 파싱, READ CAPACITY(16)로 block_size/num_blocks 획득 후 real_file_size 노출, poll(2)+iscsi_which_events→iscsi_service→iscsi_cb의 이벤트 드리븐 비동기 계약, 단일 잡 스레드 직렬화), #include 4종 사유(fio.h/optgroup.h/iscsi/iscsi.h/iscsi/scsi-lowlevel.h/poll.h) 인라인, iscsi_task 3필드·iscsi_lun 5필드·iscsi_info 5필드·iscsi_options 2필드 §4 멀티라인(설정자/독자/값범위/동기화), fio_option 테이블 모든 필드 인라인, 전 함수 §2(setup_lun/setup/init/cleanup_lun/cleanup/prep/open_file/close_file/iscsi_cb/queue/getevents/event/register/unregister), READ16/WRITE16/SYNCHRONIZECACHE16 CDB 파라미터(rdprotect/wrprotect/dpo/fua/group_number 0 의미, LBA=offset/block_size 환산) 인라인, scsi_task_add_data_in/out_buffer의 zero-copy 근거, ddir_sync 분기, iscsi_which_events로 POLLIN/POLLOUT 마스크 계산, iscsi_service(revents), iscsi_cb가 complete_events[nr_events++] 축적하는 단일스레드 경로, FIO_Q_QUEUED vs FIO_Q_COMPLETED 에러 경로, ioengine_ops 모든 필드 §4(FIO_SYNCIO|FIO_DISKLESSIO|FIO_NODISKUTIL 플래그 풀이), fio_init/fio_exit GCC constructor/destructor 설명 | 16 → 신기준
- 2026-04-15 | io_u.c | io_u 생명주기 허브 신기준 상단 블록 재작성: 상태전이 ASCII 다이어그램(free→prepped→in_flight→completed→(verify?)→free) + 3큐(io_u_freelist/io_u_all/io_u_requeues) 전이, 오프셋/길이/방향 결정 알고리즘 전수(set_rw_ddir/get_rw_ddir/rate_ddir/get_rand_ddir rwmix 경로, get_next_offset→순차/랜덤 분기, __get_next_rand_offset 변종 6종 — DEFAULT(uniform+LFSR)/ZIPF/PARETO/GAUSS(정규분포 dev)/ZONED[_ABS]/SPRANDOM, get_next_buflen의 bsrange/bssplit CDF/FIO_RAND_BS 분기), axmap_isset 커버리지, trimwrite/verify 디스패치(check_get_trim/check_get_verify/verify_list), 완료 회계(io_u_sync_complete/io_u_queued_complete→ios_completed→io_completed→account_io_completion의 clat/slat/lat/bw→stat.c add_*_sample, trim_block_info iolog 무효화, io_u_mark_lat_{n,u,m}sec·io_u_mark_submit/complete/depth 히스토그램 버킷), save_buf_state/get_buf_state의 verify 시드 보존, io_u 플래그 7비트 전수(FREE/FLIGHT/NO_UNACCOUNT/TRIMMED/BARRIER/VER_LIST/BUSY_OK) 설정자/독자 명시, ddir 7종(READ/WRITE/TRIM/SYNC/DATASYNC/SYNC_FILE_RANGE/WAIT/INVAL) 풀이, backend.c/ioengines.c/stat.c/iolog.c/verify.c/lib/rand·axmap·lfsr/zbd/sprandom/trim 연결 도식, 주요 함수 요약. io_completion_data 4필드 §4 멀티라인 재작성(nr/error/bytes_done/time 설정자·독자·값범위·동기화), #include 12개 전수 인라인 사유. 기존 함수별 §2 및 라인 인라인은 후속 패스 대상으로 남김(3684라인, 83함수) — 상단 블록·진입 구조체·전역 헤더는 신기준 충족 | 433 → 신기준(상단·최상위 구조체)
- 2026-04-15 | ioengines.c | I/O 엔진 플러그인 계약 허브 신기준 재작성: 상단 4섹션을 "플러그인 계약 허브" 관점으로 전면 확장(backend.c ↔ engines/*.c 사이 유일한 디스패치 계층, 잡 스레드 컨텍스트, engine_list 읽기전용 전역, overlap_check 뮤텍스 offload 동기화), 엔진 로딩 경로 체인(load_ioengine → find_ioengine | dlopen → dlsym "<name>"/"ioengine"/get_ioengine 팩토리, FIO_EXT_ENG_DIR dlopen_external 폴백, ops->dlhandle 참조 카운트), io_u 생명주기 디스패치 전수 설명(prep/queue/commit/getevents/event 각 단계의 카운터 이동 — io_u_queued→io_u_in_flight→감소), FIO_Q_COMPLETED/QUEUED/BUSY 반환값 의미와 BUSY 시 io_issues/issue_bytes 롤백 + IO_U_F_FLIGHT clear, ioengine_ops 14개 플래그 비트 전수 풀이(SYNCIO/DISKLESSIO/NOIO/RAWIO/MEMALIGN/PIPEIO/BARRIER/UNIDIR/NOEXTEND/NODISKUTIL/ASYNCIO_SYNC_TRIM/ASYNCIO_SETS_ISSUE_TIME/RO/MULTI_RANGE_TRIM/NO_OFFLOAD), io_u_mark_submit/complete/depth 계측 훅 호출 시점(commit 유무에 따른 분기), issue_time 기록 경로 분기(SYNCIO는 queue 진입 시, ASYNCIO_SETS_ISSUE_TIME이면 엔진 자체 기록), register_ioengine/unregister_ioengine constructor 계약, close_ioengine vs free_ioengine 정리 경로 차이. 기존 함수별 §2와 인라인은 이미 신기준 수준으로 구비되어 상단 블록만 전면 재작성 | 46 → 신기준
- 2026-04-15 | fio.c | 최상위 main() 진입점 신기준 재작성: 상단 4섹션(얇은 지휘봉 역할 근거·_start→__libc_start_main→main→initialize_fio→sk_key→parse_options→setvbuf→fio_time_init→nr_clients 분기→fio_backend|fio_handle_clients→destroy_sk_key→deinitialize_fio 체인, 시그널(SIGTERM/SIGINT/SIGHUP/SIGUSR1/SIGUSR2)·terminate_threads·show_run_stats·잡 스폰(pthread_create/fork)·아키텍처 sanity는 하위 모듈 위임 명시, 공유 전역 nr_clients/fio_client_ops/thread_data[] 계약), #include "fio.h"가 setvbuf/_IOLBF/stdout을 간접 공급하는 이유, main §2(argc/argv/envp 각 출처와 envp가 POSIX 비표준이지만 리눅스 libc 지원인 점, return 코드가 shell $?로 노출), 모든 실행 라인 인라인(ret=1 초기값이 기본 실패인 이유, _IOLBF로 다중 스레드 stdout 원자화 원리, fio_time_init의 TSC 보정, set_genesis_time의 분산 시각 동기화, goto done_key vs done 부분정리 경계, deinitialize_fio가 smalloc/mutex/ioengine dlclose 회수) | 1 → 신기준
- 2026-04-15 | engines/http.c | libcurl easy 기반 S3/Swift/WebDAV 오브젝트 엔진 신기준 재작성: 상단 4섹션(FIO_SYNCIO+FIO_DISKLESSIO 계약·ddir→PUT/GET/DELETE 매핑·object_mode block/range·SigV4 헤더 서명 체인·OpenSSL HMAC_CTX opaque/투명 분기), 모든 #include/매크로/pragma 인라인, enum 전 상수(HTTP_WEBDAV/S3/SWIFT, HTTPS_OFF/ON/INSECURE, OBJECT_BLOCK/RANGE) 풀이, http_data/http_options 16필드·http_curl_stream 3필드 §4, 모든 함수 §2(_aws_uriencode/_conv_hex/_gen_hex_sha256/_gen_hex_md5/_conv_base64_encode/_gen_base64_md5/_hmac/_curl_trace/_add_aws_auth_header/_add_swift_header/_append_range_header/fio_http_cleanup/_http_read/_http_write/_http_seek/fio_http_queue/fio_http_event/fio_http_getevents/fio_http_setup/fio_http_open/fio_http_invalidate/register/unregister), SigV4 5단계 HMAC 체인(kSecret→kDate→kRegion→kService→kSigning→signature) 각 라인 인라인, SSE-C(알고리즘/Base64 키/키-MD5) 및 STS security token 헤더 처리, Range 헤더 end-inclusive 클램핑, 404→zero-read/DELETE 관대 처리, CURLOPT_UPLOAD↔HTTPGET↔CUSTOMREQUEST 전환 근거, use_thread=1 강제 이유, ioengine_ops 전 필드 §4 | 19 → 신기준
- 2026-04-15 | engines/cmdprio.c | libaio/io_uring/sg 공유 I/O 우선순위 유틸 신기준 재작성: 상단 4섹션(ioprio_set(2) 16비트 인코딩/IOPRIO_CLASS_RT·BE·IDLE/IOPRIO_HINT_DEV_DURATION_LIMIT 스펙, PERC vs BSSPLIT 모드 문법, cmdprio_ops를 통한 엔진 연계, 호출 체인 init→parse_and_gen→gen_perc|bssplit / queue→set_ioprio→percentage→rand_between), cmdprio_parse_result·cmdprio_values 전 필드 §4(수명/설정자/독자/동기화), 전 함수(find/assign_clat_prio_index, init_cmdprio_values, init_ts_clat_prio, fill_bsprio, generate_bsprio_desc, bssplit_ddir/parse, percentage, set_ioprio, gen_perc, parse_and_gen_bssplit, parse_and_gen, cleanup, init) §2, 모든 실행 라인 인라인(CDF 누적 추첨, implicit_cmdprio 합성, td_read/td_write skip, err goto 자원 회수, mutually exclusive 검증) | 17 → 신기준
- 2026-04-15 | engines/dfs.c | DAOS File System 엔진 신기준 재작성: 상단 4섹션(DAOS 풀/컨테이너/DFS 마운트 체인, 프로세스 전역 핸들 공유 + daos_mutex 직렬화, EQ 기반 비동기 완료, container_of(evp, daos_iou, ev) 역매핑), 전역 poh/coh/cid/daosfs/daos_mutex/daos_initialized/num_threads 각 인라인, daos_iou 6필드·daos_data 5필드·daos_fio_options 6필드 §4, 모든 함수 §2(global_init/cleanup/setup/init/cleanup/get_file_size/open/close/unlink/invalidate/io_u_init/free/event/getevents/queue/prep/register/unregister), DAOS_API_VERSION #ifdef 분기 이유, daos_eq_poll NOWAIT 폴 루프와 event_fini, FIO_Q_QUEUED/BUSY/COMPLETED 계약 인라인, ioengine_ops 전 필드 §4 | 13 → 신기준
- 2026-04-15 | engines/cpu.c | CPU-burner 엔진 신기준 재작성: FIO_SYNCIO/DISKLESSIO/NOIO 플래그 조합 풀이, noisy-neighbor 시뮬레이션 근거, noop(usec_spin)·qsort(4-pass) 모드, cpuload=cpucycle/(cpucycle+thinktime) 수식, do_qsort 자기보정 루프, MWC RNG 상태 §4, cpu_options 6필드 §4, ioengine_ops 전 필드 §4, 모든 실행 라인 인라인 | 14 → 신기준
- 2026-04-15 | engines/falloc.c | fallocate(2) 모드 플래그(FALLOC_FL_KEEP_SIZE/PUNCH_HOLE) ddir별 매핑(READ=KEEP_SIZE/WRITE=0/TRIM=PUNCH_HOLE|KEEP_SIZE), O_RDWR|O_CREAT open 근거(TRIM 필수), filehash 경쟁 재시도(open_again goto), FIO_SYNCIO+FIO_SYNCFS 플래그, ioengine_ops 전 필드 §4, 모든 실행 라인 인라인 | 51 → 신기준
- 2026-04-15 | engines/ftruncate.c | ftruncate(2)로 WRITE를 파일 크기 변경에 매핑해 FS 메타데이터 비용 측정, ioengine_ops 전 필드 §4(FIO_SYNCIO/FAKEIO/SYNCFS 플래그 풀이), queue/register/unregister 전 함수 §2, ddir 분기·errno 전파·FIO_Q_COMPLETED 계약 인라인 | 27 → 신기준(188 라인)
- 2026-04-13 | engines/exec.c | fork/execvp/waitpid/SIGTERM·SIGKILL·dup2 커널 의미, exec_options 6필드 §4, 모든 함수 §2, 모든 실행 라인 인라인 | 9 → 443→690여 라인 확장
- 2026-04-13 | engines/dev-dax.c | DAX 페이지캐시 우회·pmem_memcpy_persist/CLWB/SFENCE 영속성, sysfs 파싱 인라인, ioengine_ops 필드별 설명 | 7 → 신기준
- 2026-04-13 | engines/null.c | **레퍼런스 템플릿**: 상단에 ioengine_ops 계약 요약표, C++/extern "C" 래퍼·constructor/destructor·NullData 메서드 전 라인 인라인, ioengine_ops 전 필드 §4 | 74 → 신기준
- 2026-04-13 | engines/fileoperations.c | "데이터 I/O 아닌 FS 메타연산 측정" 명시, FIO_FILESTAT_* 매크로, fc_data·filestat_options·fio_engine 필드 §4, 플래그 비트(FIO_DISKLESSIO/SYNCIO/FAKEIO/SYNCFS/NOSTATS/NOFILEHASH) 풀이 | 3 → 신기준
- 2026-04-13 | engines/glusterfs.c | sync/async 공유 공통 유틸, glfs_info 전 필드 §4, 9개 함수 §2 | 얕음 → 신기준
- 2026-04-13 | engines/glusterfs_sync.c | CONFIG_GF_NEW_API #ifdef 분기, FIO_SYNCIO+FIO_DISKLESSIO 플래그, LAST_POS 센티넬, FIO_Q_COMPLETED 계약 | 2 → 신기준
- 2026-04-13 | engines/glusterfs_async.c | 비동기 계약(queue→FIO_Q_QUEUED/event/getevents/io_u_init/io_u_free), fio_gf_iou 두 필드 §4, gf_async_cb 콜백 경로 | 4/239 → 500여 라인
- 2026-04-13 | (보강 작업 없음) | 현황 스냅샷 작성 — 본 섹션 재정비. 이전의 "267/368 구기준 완료" 표기를 파일별 ◐/●/○ 체계로 이관.

### 다음 착수 대상 (Next Up)

> 다음 세션에서 이 프로젝트에 동일 명령("주석 작업해줘")을 받으면 **우선 이 포인터부터 확인**한다.
> 포인터가 비었거나 방금 완료된 파일이면 "우선순위" 규칙에 따라 다음 파일을 선택하고 이 포인터를 갱신한다.

**현재 진행 상황**: engines/ 43개 중 **38개 ● 완료** (glusterfs_async/sync, glusterfs, fileoperations, exec, null, dev-dax, ftruncate, falloc, cpu, dfs, cmdprio, http, libiscsi, libcufile, libhdfs, libblkio, skeleton_external, mmap, nbd, posixaio, libpmem, e4defrag, windowsaio, sync, nfs, libzbc, rbd, solarisaio, libaio, splice, xnvme, rdma, nvme, ime, mtd, net, io_uring + nvme.h + cmdprio.h + gfapi.h 헤더 동시). **engines/ 디렉토리 .c 파일은 전부 신기준 완료** (남은 것 없음 — 이전 "10개 ◐"은 2026-04-18 세션에서 전원 마감).

**다음 세션 우선순위** — engines/ 완료되었으므로 코어로 이동:
- **1순위 (핵심 코어 — 얕은 것부터)**: `backend.c` (1743/5052, 34% — 2026-04-18 70-80% 보강 후 ◐ 유지, do_io/thread_main/run_threads 본문 §3 전수 인라인 1회 더 패스 필요), `io_u.c` (상단 블록만 신기준, 83 함수 §2 미완 + 3684 라인 인라인 미완), `stat.c` (908/5131, 18% — 2026-04-18 상단 블록+함수 §2 전수 완료, 대형 출력 함수 본문 §3 인라인 1회 더 패스 필요), `iolog.c` (239/2175, 11%), `options.c` (1382/8311, 16.6%), `server.c` (1387/4669, 29.7% — 2026-04-18 2차 보강으로 handle_command opcode dispatch 12 case + 80+ 함수 §2 완료, 후속 1회 패스 필요). `init.c` 는 2026-04-18 완료(●, 1652/5100). `verify.c` 는 2026-04-18 완료(●, 1348/3487).
- **2순위 (체크섬/해시/lib)**: `crc/` (33), `lib/` (45), `oslib/` (26).
- **3순위**: `arch/` (18), `t/` (19), `unittests/` (10), `os/` (23), `profiles/`.
- **참고 헤더**: engines/ 내 나머지 헤더(.h)는 독립 가치보다 .c와 동반 처리 시 효율적.

**코어 블록 착수 전제조건 충족**: engines 43개 **전원 ●** 달성(이전 목표 10개 초과). **fio.c·ioengines.c·io_u.c(부분)는 2026-04-15 신기준 완료** — 다음 코어 착수 1순위는 `init.c`(parse_options, fio.c가 직접 호출) → `backend.c` → `io_u.c` 후속 패스(83개 함수 §2와 3684라인 인라인 보강).

**신기준 레퍼런스 파일**: `engines/null.c` (ioengine_ops 콜백 계약 요약표 포함). 다른 엔진 작업 시 이 파일을 참고 예시로 제시할 것.

**세션별 권장 배치 크기**: 소형 파일(<100 라인 한국어 추가 전) 3~5개 병렬, 중형(100~300) 2~3개 병렬, 대형(300+) 1개씩 순차. 토큰 85% 상한을 넘지 않도록 웨이브 간 갱신 체크.

## 빌드 방법

```bash
./configure
make
```

## 참고

- fio 공식 문서: HOWTO.rst
- fio 옵션 목록: `fio --enghelp`, `fio --cmdhelp`
- gfio (GUI): GTK+ 기반 그래픽 프론트엔드 (gfio.c, gclient.c 등)
- NVMe 스펙: NVM Express Base Specification 1.x
- io_uring: Linux Kernel UAPI `linux/io_uring.h`
